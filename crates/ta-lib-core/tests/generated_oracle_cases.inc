/* Generated by ta-lib-codegen. */
} else if (strcmp(argv[1], "matrix_default_accbands") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        double outRealUpperBand[256] = {0.0};
        double outRealMiddleBand[256] = {0.0};
        double outRealLowerBand[256] = {0.0};
        code = TA_ACCBANDS(0, 15, High, Low, Close, 20, &outBegIdx, &outNbElement, outRealUpperBand, outRealMiddleBand, outRealLowerBand);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %.17g", outRealUpperBand[i]);
        }
        printf(" |");
        for (int i = 0; i < outNbElement; i++) {
            printf(" %.17g", outRealMiddleBand[i]);
        }
        printf(" |");
        for (int i = 0; i < outNbElement; i++) {
            printf(" %.17g", outRealLowerBand[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_boundary_accbands") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int boundaryEndIdx = TA_ACCBANDS_Lookback(20);
        if (boundaryEndIdx < 0) boundaryEndIdx = 0;
        if (boundaryEndIdx > 255) boundaryEndIdx = 255;
        double outRealUpperBand[256] = {0.0};
        double outRealMiddleBand[256] = {0.0};
        double outRealLowerBand[256] = {0.0};
        code = TA_ACCBANDS(0, boundaryEndIdx, High, Low, Close, 20, &outBegIdx, &outNbElement, outRealUpperBand, outRealMiddleBand, outRealLowerBand);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %.17g", outRealUpperBand[i]);
        }
        printf(" |");
        for (int i = 0; i < outNbElement; i++) {
            printf(" %.17g", outRealMiddleBand[i]);
        }
        printf(" |");
        for (int i = 0; i < outNbElement; i++) {
            printf(" %.17g", outRealLowerBand[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_seeded_accbands") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        double outRealUpperBand[256] = {0.0};
        double outRealMiddleBand[256] = {0.0};
        double outRealLowerBand[256] = {0.0};
        code = TA_ACCBANDS(0, 127, High, Low, Close, 20, &outBegIdx, &outNbElement, outRealUpperBand, outRealMiddleBand, outRealLowerBand);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %.17g", outRealUpperBand[i]);
        }
        printf(" |");
        for (int i = 0; i < outNbElement; i++) {
            printf(" %.17g", outRealMiddleBand[i]);
        }
        printf(" |");
        for (int i = 0; i < outNbElement; i++) {
            printf(" %.17g", outRealLowerBand[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_variant_accbands") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        double outRealUpperBand[256] = {0.0};
        double outRealMiddleBand[256] = {0.0};
        double outRealLowerBand[256] = {0.0};
        code = TA_ACCBANDS(0, 127, High, Low, Close, 5, &outBegIdx, &outNbElement, outRealUpperBand, outRealMiddleBand, outRealLowerBand);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %.17g", outRealUpperBand[i]);
        }
        printf(" |");
        for (int i = 0; i < outNbElement; i++) {
            printf(" %.17g", outRealMiddleBand[i]);
        }
        printf(" |");
        for (int i = 0; i < outNbElement; i++) {
            printf(" %.17g", outRealLowerBand[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_default_acos") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        double outReal[256] = {0.0};
        code = TA_ACOS(0, 15, inReal, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_boundary_acos") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int boundaryEndIdx = TA_ACOS_Lookback();
        if (boundaryEndIdx < 0) boundaryEndIdx = 0;
        if (boundaryEndIdx > 255) boundaryEndIdx = 255;
        double outReal[256] = {0.0};
        code = TA_ACOS(0, boundaryEndIdx, inReal, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_seeded_acos") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        double outReal[256] = {0.0};
        code = TA_ACOS(0, 127, inReal, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_variant_acos") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        double outReal[256] = {0.0};
        code = TA_ACOS(0, 127, inReal, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_default_ad") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        double outReal[256] = {0.0};
        code = TA_AD(0, 15, High, Low, Close, Volume, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_boundary_ad") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int boundaryEndIdx = TA_AD_Lookback();
        if (boundaryEndIdx < 0) boundaryEndIdx = 0;
        if (boundaryEndIdx > 255) boundaryEndIdx = 255;
        double outReal[256] = {0.0};
        code = TA_AD(0, boundaryEndIdx, High, Low, Close, Volume, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_seeded_ad") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        double outReal[256] = {0.0};
        code = TA_AD(0, 127, High, Low, Close, Volume, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_variant_ad") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        double outReal[256] = {0.0};
        code = TA_AD(0, 127, High, Low, Close, Volume, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_default_add") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        double outReal[256] = {0.0};
        code = TA_ADD(0, 15, inReal0, inReal1, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_boundary_add") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int boundaryEndIdx = TA_ADD_Lookback();
        if (boundaryEndIdx < 0) boundaryEndIdx = 0;
        if (boundaryEndIdx > 255) boundaryEndIdx = 255;
        double outReal[256] = {0.0};
        code = TA_ADD(0, boundaryEndIdx, inReal0, inReal1, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_seeded_add") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        double outReal[256] = {0.0};
        code = TA_ADD(0, 127, inReal0, inReal1, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_variant_add") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        double outReal[256] = {0.0};
        code = TA_ADD(0, 127, inReal0, inReal1, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_default_ad_osc") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        double outReal[256] = {0.0};
        code = TA_ADOSC(0, 15, High, Low, Close, Volume, 3, 10, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_boundary_ad_osc") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int boundaryEndIdx = TA_ADOSC_Lookback(3, 10);
        if (boundaryEndIdx < 0) boundaryEndIdx = 0;
        if (boundaryEndIdx > 255) boundaryEndIdx = 255;
        double outReal[256] = {0.0};
        code = TA_ADOSC(0, boundaryEndIdx, High, Low, Close, Volume, 3, 10, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_seeded_ad_osc") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        double outReal[256] = {0.0};
        code = TA_ADOSC(0, 127, High, Low, Close, Volume, 3, 10, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_variant_ad_osc") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        double outReal[256] = {0.0};
        code = TA_ADOSC(0, 127, High, Low, Close, Volume, 8, 17, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_default_adx") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        double outReal[256] = {0.0};
        code = TA_ADX(0, 15, High, Low, Close, 14, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_boundary_adx") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int boundaryEndIdx = TA_ADX_Lookback(14);
        if (boundaryEndIdx < 0) boundaryEndIdx = 0;
        if (boundaryEndIdx > 255) boundaryEndIdx = 255;
        double outReal[256] = {0.0};
        code = TA_ADX(0, boundaryEndIdx, High, Low, Close, 14, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_seeded_adx") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        double outReal[256] = {0.0};
        code = TA_ADX(0, 127, High, Low, Close, 14, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_variant_adx") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        double outReal[256] = {0.0};
        code = TA_ADX(0, 127, High, Low, Close, 5, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_default_adxr") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        double outReal[256] = {0.0};
        code = TA_ADXR(0, 15, High, Low, Close, 14, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_boundary_adxr") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int boundaryEndIdx = TA_ADXR_Lookback(14);
        if (boundaryEndIdx < 0) boundaryEndIdx = 0;
        if (boundaryEndIdx > 255) boundaryEndIdx = 255;
        double outReal[256] = {0.0};
        code = TA_ADXR(0, boundaryEndIdx, High, Low, Close, 14, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_seeded_adxr") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        double outReal[256] = {0.0};
        code = TA_ADXR(0, 127, High, Low, Close, 14, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_variant_adxr") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        double outReal[256] = {0.0};
        code = TA_ADXR(0, 127, High, Low, Close, 5, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_default_apo") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        double outReal[256] = {0.0};
        code = TA_APO(0, 15, inReal, 12, 26, 0, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_boundary_apo") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int boundaryEndIdx = TA_APO_Lookback(12, 26, 0);
        if (boundaryEndIdx < 0) boundaryEndIdx = 0;
        if (boundaryEndIdx > 255) boundaryEndIdx = 255;
        double outReal[256] = {0.0};
        code = TA_APO(0, boundaryEndIdx, inReal, 12, 26, 0, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_seeded_apo") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        double outReal[256] = {0.0};
        code = TA_APO(0, 127, inReal, 12, 26, 0, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_variant_apo") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        double outReal[256] = {0.0};
        code = TA_APO(0, 127, inReal, 8, 17, 1, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_default_aroon") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        double outAroonDown[256] = {0.0};
        double outAroonUp[256] = {0.0};
        code = TA_AROON(0, 15, High, Low, 14, &outBegIdx, &outNbElement, outAroonDown, outAroonUp);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %.17g", outAroonDown[i]);
        }
        printf(" |");
        for (int i = 0; i < outNbElement; i++) {
            printf(" %.17g", outAroonUp[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_boundary_aroon") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int boundaryEndIdx = TA_AROON_Lookback(14);
        if (boundaryEndIdx < 0) boundaryEndIdx = 0;
        if (boundaryEndIdx > 255) boundaryEndIdx = 255;
        double outAroonDown[256] = {0.0};
        double outAroonUp[256] = {0.0};
        code = TA_AROON(0, boundaryEndIdx, High, Low, 14, &outBegIdx, &outNbElement, outAroonDown, outAroonUp);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %.17g", outAroonDown[i]);
        }
        printf(" |");
        for (int i = 0; i < outNbElement; i++) {
            printf(" %.17g", outAroonUp[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_seeded_aroon") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        double outAroonDown[256] = {0.0};
        double outAroonUp[256] = {0.0};
        code = TA_AROON(0, 127, High, Low, 14, &outBegIdx, &outNbElement, outAroonDown, outAroonUp);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %.17g", outAroonDown[i]);
        }
        printf(" |");
        for (int i = 0; i < outNbElement; i++) {
            printf(" %.17g", outAroonUp[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_variant_aroon") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        double outAroonDown[256] = {0.0};
        double outAroonUp[256] = {0.0};
        code = TA_AROON(0, 127, High, Low, 5, &outBegIdx, &outNbElement, outAroonDown, outAroonUp);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %.17g", outAroonDown[i]);
        }
        printf(" |");
        for (int i = 0; i < outNbElement; i++) {
            printf(" %.17g", outAroonUp[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_default_aroon_osc") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        double outReal[256] = {0.0};
        code = TA_AROONOSC(0, 15, High, Low, 14, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_boundary_aroon_osc") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int boundaryEndIdx = TA_AROONOSC_Lookback(14);
        if (boundaryEndIdx < 0) boundaryEndIdx = 0;
        if (boundaryEndIdx > 255) boundaryEndIdx = 255;
        double outReal[256] = {0.0};
        code = TA_AROONOSC(0, boundaryEndIdx, High, Low, 14, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_seeded_aroon_osc") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        double outReal[256] = {0.0};
        code = TA_AROONOSC(0, 127, High, Low, 14, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_variant_aroon_osc") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        double outReal[256] = {0.0};
        code = TA_AROONOSC(0, 127, High, Low, 5, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_default_asin") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        double outReal[256] = {0.0};
        code = TA_ASIN(0, 15, inReal, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_boundary_asin") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int boundaryEndIdx = TA_ASIN_Lookback();
        if (boundaryEndIdx < 0) boundaryEndIdx = 0;
        if (boundaryEndIdx > 255) boundaryEndIdx = 255;
        double outReal[256] = {0.0};
        code = TA_ASIN(0, boundaryEndIdx, inReal, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_seeded_asin") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        double outReal[256] = {0.0};
        code = TA_ASIN(0, 127, inReal, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_variant_asin") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        double outReal[256] = {0.0};
        code = TA_ASIN(0, 127, inReal, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_default_atan") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        double outReal[256] = {0.0};
        code = TA_ATAN(0, 15, inReal, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_boundary_atan") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int boundaryEndIdx = TA_ATAN_Lookback();
        if (boundaryEndIdx < 0) boundaryEndIdx = 0;
        if (boundaryEndIdx > 255) boundaryEndIdx = 255;
        double outReal[256] = {0.0};
        code = TA_ATAN(0, boundaryEndIdx, inReal, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_seeded_atan") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        double outReal[256] = {0.0};
        code = TA_ATAN(0, 127, inReal, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_variant_atan") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        double outReal[256] = {0.0};
        code = TA_ATAN(0, 127, inReal, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_default_atr") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        double outReal[256] = {0.0};
        code = TA_ATR(0, 15, High, Low, Close, 14, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_boundary_atr") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int boundaryEndIdx = TA_ATR_Lookback(14);
        if (boundaryEndIdx < 0) boundaryEndIdx = 0;
        if (boundaryEndIdx > 255) boundaryEndIdx = 255;
        double outReal[256] = {0.0};
        code = TA_ATR(0, boundaryEndIdx, High, Low, Close, 14, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_seeded_atr") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        double outReal[256] = {0.0};
        code = TA_ATR(0, 127, High, Low, Close, 14, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_variant_atr") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        double outReal[256] = {0.0};
        code = TA_ATR(0, 127, High, Low, Close, 5, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_default_avg_price") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        double outReal[256] = {0.0};
        code = TA_AVGPRICE(0, 15, Open, High, Low, Close, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_boundary_avg_price") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int boundaryEndIdx = TA_AVGPRICE_Lookback();
        if (boundaryEndIdx < 0) boundaryEndIdx = 0;
        if (boundaryEndIdx > 255) boundaryEndIdx = 255;
        double outReal[256] = {0.0};
        code = TA_AVGPRICE(0, boundaryEndIdx, Open, High, Low, Close, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_seeded_avg_price") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        double outReal[256] = {0.0};
        code = TA_AVGPRICE(0, 127, Open, High, Low, Close, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_variant_avg_price") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        double outReal[256] = {0.0};
        code = TA_AVGPRICE(0, 127, Open, High, Low, Close, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_default_avg_dev") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        double outReal[256] = {0.0};
        code = TA_AVGDEV(0, 15, inReal, 14, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_boundary_avg_dev") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int boundaryEndIdx = TA_AVGDEV_Lookback(14);
        if (boundaryEndIdx < 0) boundaryEndIdx = 0;
        if (boundaryEndIdx > 255) boundaryEndIdx = 255;
        double outReal[256] = {0.0};
        code = TA_AVGDEV(0, boundaryEndIdx, inReal, 14, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_seeded_avg_dev") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        double outReal[256] = {0.0};
        code = TA_AVGDEV(0, 127, inReal, 14, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_variant_avg_dev") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        double outReal[256] = {0.0};
        code = TA_AVGDEV(0, 127, inReal, 5, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_default_bbands") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        double outRealUpperBand[256] = {0.0};
        double outRealMiddleBand[256] = {0.0};
        double outRealLowerBand[256] = {0.0};
        code = TA_BBANDS(0, 15, inReal, 5, 2.000000e+0, 2.000000e+0, 0, &outBegIdx, &outNbElement, outRealUpperBand, outRealMiddleBand, outRealLowerBand);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %.17g", outRealUpperBand[i]);
        }
        printf(" |");
        for (int i = 0; i < outNbElement; i++) {
            printf(" %.17g", outRealMiddleBand[i]);
        }
        printf(" |");
        for (int i = 0; i < outNbElement; i++) {
            printf(" %.17g", outRealLowerBand[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_boundary_bbands") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int boundaryEndIdx = TA_BBANDS_Lookback(5, 2.000000e+0, 2.000000e+0, 0);
        if (boundaryEndIdx < 0) boundaryEndIdx = 0;
        if (boundaryEndIdx > 255) boundaryEndIdx = 255;
        double outRealUpperBand[256] = {0.0};
        double outRealMiddleBand[256] = {0.0};
        double outRealLowerBand[256] = {0.0};
        code = TA_BBANDS(0, boundaryEndIdx, inReal, 5, 2.000000e+0, 2.000000e+0, 0, &outBegIdx, &outNbElement, outRealUpperBand, outRealMiddleBand, outRealLowerBand);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %.17g", outRealUpperBand[i]);
        }
        printf(" |");
        for (int i = 0; i < outNbElement; i++) {
            printf(" %.17g", outRealMiddleBand[i]);
        }
        printf(" |");
        for (int i = 0; i < outNbElement; i++) {
            printf(" %.17g", outRealLowerBand[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_seeded_bbands") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        double outRealUpperBand[256] = {0.0};
        double outRealMiddleBand[256] = {0.0};
        double outRealLowerBand[256] = {0.0};
        code = TA_BBANDS(0, 127, inReal, 5, 2.000000e+0, 2.000000e+0, 0, &outBegIdx, &outNbElement, outRealUpperBand, outRealMiddleBand, outRealLowerBand);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %.17g", outRealUpperBand[i]);
        }
        printf(" |");
        for (int i = 0; i < outNbElement; i++) {
            printf(" %.17g", outRealMiddleBand[i]);
        }
        printf(" |");
        for (int i = 0; i < outNbElement; i++) {
            printf(" %.17g", outRealLowerBand[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_variant_bbands") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        double outRealUpperBand[256] = {0.0};
        double outRealMiddleBand[256] = {0.0};
        double outRealLowerBand[256] = {0.0};
        code = TA_BBANDS(0, 127, inReal, 5, 2.000000e+0, 2.000000e+0, 1, &outBegIdx, &outNbElement, outRealUpperBand, outRealMiddleBand, outRealLowerBand);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %.17g", outRealUpperBand[i]);
        }
        printf(" |");
        for (int i = 0; i < outNbElement; i++) {
            printf(" %.17g", outRealMiddleBand[i]);
        }
        printf(" |");
        for (int i = 0; i < outNbElement; i++) {
            printf(" %.17g", outRealLowerBand[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_default_beta") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        double outReal[256] = {0.0};
        code = TA_BETA(0, 15, inReal0, inReal1, 5, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_boundary_beta") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int boundaryEndIdx = TA_BETA_Lookback(5);
        if (boundaryEndIdx < 0) boundaryEndIdx = 0;
        if (boundaryEndIdx > 255) boundaryEndIdx = 255;
        double outReal[256] = {0.0};
        code = TA_BETA(0, boundaryEndIdx, inReal0, inReal1, 5, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_seeded_beta") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        double outReal[256] = {0.0};
        code = TA_BETA(0, 127, inReal0, inReal1, 5, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_variant_beta") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        double outReal[256] = {0.0};
        code = TA_BETA(0, 127, inReal0, inReal1, 5, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_default_bop") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        double outReal[256] = {0.0};
        code = TA_BOP(0, 15, Open, High, Low, Close, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_boundary_bop") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int boundaryEndIdx = TA_BOP_Lookback();
        if (boundaryEndIdx < 0) boundaryEndIdx = 0;
        if (boundaryEndIdx > 255) boundaryEndIdx = 255;
        double outReal[256] = {0.0};
        code = TA_BOP(0, boundaryEndIdx, Open, High, Low, Close, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_seeded_bop") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        double outReal[256] = {0.0};
        code = TA_BOP(0, 127, Open, High, Low, Close, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_variant_bop") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        double outReal[256] = {0.0};
        code = TA_BOP(0, 127, Open, High, Low, Close, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_default_cci") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        double outReal[256] = {0.0};
        code = TA_CCI(0, 15, High, Low, Close, 14, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_boundary_cci") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int boundaryEndIdx = TA_CCI_Lookback(14);
        if (boundaryEndIdx < 0) boundaryEndIdx = 0;
        if (boundaryEndIdx > 255) boundaryEndIdx = 255;
        double outReal[256] = {0.0};
        code = TA_CCI(0, boundaryEndIdx, High, Low, Close, 14, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_seeded_cci") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        double outReal[256] = {0.0};
        code = TA_CCI(0, 127, High, Low, Close, 14, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_variant_cci") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        double outReal[256] = {0.0};
        code = TA_CCI(0, 127, High, Low, Close, 5, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_default_cdl_2_crows") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int outInteger[256] = {0};
        code = TA_CDL2CROWS(0, 15, Open, High, Low, Close, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_boundary_cdl_2_crows") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int boundaryEndIdx = TA_CDL2CROWS_Lookback();
        if (boundaryEndIdx < 0) boundaryEndIdx = 0;
        if (boundaryEndIdx > 255) boundaryEndIdx = 255;
        int outInteger[256] = {0};
        code = TA_CDL2CROWS(0, boundaryEndIdx, Open, High, Low, Close, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_seeded_cdl_2_crows") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        int outInteger[256] = {0};
        code = TA_CDL2CROWS(0, 127, Open, High, Low, Close, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_variant_cdl_2_crows") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        int outInteger[256] = {0};
        code = TA_CDL2CROWS(0, 127, Open, High, Low, Close, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_default_cdl_3_black_crows") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int outInteger[256] = {0};
        code = TA_CDL3BLACKCROWS(0, 15, Open, High, Low, Close, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_boundary_cdl_3_black_crows") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int boundaryEndIdx = TA_CDL3BLACKCROWS_Lookback();
        if (boundaryEndIdx < 0) boundaryEndIdx = 0;
        if (boundaryEndIdx > 255) boundaryEndIdx = 255;
        int outInteger[256] = {0};
        code = TA_CDL3BLACKCROWS(0, boundaryEndIdx, Open, High, Low, Close, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_seeded_cdl_3_black_crows") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        int outInteger[256] = {0};
        code = TA_CDL3BLACKCROWS(0, 127, Open, High, Low, Close, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_variant_cdl_3_black_crows") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        int outInteger[256] = {0};
        code = TA_CDL3BLACKCROWS(0, 127, Open, High, Low, Close, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_default_cdl_3_inside") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int outInteger[256] = {0};
        code = TA_CDL3INSIDE(0, 15, Open, High, Low, Close, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_boundary_cdl_3_inside") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int boundaryEndIdx = TA_CDL3INSIDE_Lookback();
        if (boundaryEndIdx < 0) boundaryEndIdx = 0;
        if (boundaryEndIdx > 255) boundaryEndIdx = 255;
        int outInteger[256] = {0};
        code = TA_CDL3INSIDE(0, boundaryEndIdx, Open, High, Low, Close, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_seeded_cdl_3_inside") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        int outInteger[256] = {0};
        code = TA_CDL3INSIDE(0, 127, Open, High, Low, Close, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_variant_cdl_3_inside") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        int outInteger[256] = {0};
        code = TA_CDL3INSIDE(0, 127, Open, High, Low, Close, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_default_cdl_3_line_strike") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int outInteger[256] = {0};
        code = TA_CDL3LINESTRIKE(0, 15, Open, High, Low, Close, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_boundary_cdl_3_line_strike") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int boundaryEndIdx = TA_CDL3LINESTRIKE_Lookback();
        if (boundaryEndIdx < 0) boundaryEndIdx = 0;
        if (boundaryEndIdx > 255) boundaryEndIdx = 255;
        int outInteger[256] = {0};
        code = TA_CDL3LINESTRIKE(0, boundaryEndIdx, Open, High, Low, Close, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_seeded_cdl_3_line_strike") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        int outInteger[256] = {0};
        code = TA_CDL3LINESTRIKE(0, 127, Open, High, Low, Close, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_variant_cdl_3_line_strike") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        int outInteger[256] = {0};
        code = TA_CDL3LINESTRIKE(0, 127, Open, High, Low, Close, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_default_cdl_3_outside") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int outInteger[256] = {0};
        code = TA_CDL3OUTSIDE(0, 15, Open, High, Low, Close, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_boundary_cdl_3_outside") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int boundaryEndIdx = TA_CDL3OUTSIDE_Lookback();
        if (boundaryEndIdx < 0) boundaryEndIdx = 0;
        if (boundaryEndIdx > 255) boundaryEndIdx = 255;
        int outInteger[256] = {0};
        code = TA_CDL3OUTSIDE(0, boundaryEndIdx, Open, High, Low, Close, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_seeded_cdl_3_outside") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        int outInteger[256] = {0};
        code = TA_CDL3OUTSIDE(0, 127, Open, High, Low, Close, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_variant_cdl_3_outside") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        int outInteger[256] = {0};
        code = TA_CDL3OUTSIDE(0, 127, Open, High, Low, Close, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_default_cdl_3_stars_in_south") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int outInteger[256] = {0};
        code = TA_CDL3STARSINSOUTH(0, 15, Open, High, Low, Close, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_boundary_cdl_3_stars_in_south") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int boundaryEndIdx = TA_CDL3STARSINSOUTH_Lookback();
        if (boundaryEndIdx < 0) boundaryEndIdx = 0;
        if (boundaryEndIdx > 255) boundaryEndIdx = 255;
        int outInteger[256] = {0};
        code = TA_CDL3STARSINSOUTH(0, boundaryEndIdx, Open, High, Low, Close, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_seeded_cdl_3_stars_in_south") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        int outInteger[256] = {0};
        code = TA_CDL3STARSINSOUTH(0, 127, Open, High, Low, Close, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_variant_cdl_3_stars_in_south") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        int outInteger[256] = {0};
        code = TA_CDL3STARSINSOUTH(0, 127, Open, High, Low, Close, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_default_cdl_3_white_soldiers") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int outInteger[256] = {0};
        code = TA_CDL3WHITESOLDIERS(0, 15, Open, High, Low, Close, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_boundary_cdl_3_white_soldiers") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int boundaryEndIdx = TA_CDL3WHITESOLDIERS_Lookback();
        if (boundaryEndIdx < 0) boundaryEndIdx = 0;
        if (boundaryEndIdx > 255) boundaryEndIdx = 255;
        int outInteger[256] = {0};
        code = TA_CDL3WHITESOLDIERS(0, boundaryEndIdx, Open, High, Low, Close, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_seeded_cdl_3_white_soldiers") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        int outInteger[256] = {0};
        code = TA_CDL3WHITESOLDIERS(0, 127, Open, High, Low, Close, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_variant_cdl_3_white_soldiers") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        int outInteger[256] = {0};
        code = TA_CDL3WHITESOLDIERS(0, 127, Open, High, Low, Close, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_default_cdl_abandoned_baby") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int outInteger[256] = {0};
        code = TA_CDLABANDONEDBABY(0, 15, Open, High, Low, Close, 3.000000e-1, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_boundary_cdl_abandoned_baby") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int boundaryEndIdx = TA_CDLABANDONEDBABY_Lookback(3.000000e-1);
        if (boundaryEndIdx < 0) boundaryEndIdx = 0;
        if (boundaryEndIdx > 255) boundaryEndIdx = 255;
        int outInteger[256] = {0};
        code = TA_CDLABANDONEDBABY(0, boundaryEndIdx, Open, High, Low, Close, 3.000000e-1, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_seeded_cdl_abandoned_baby") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        int outInteger[256] = {0};
        code = TA_CDLABANDONEDBABY(0, 127, Open, High, Low, Close, 3.000000e-1, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_variant_cdl_abandoned_baby") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        int outInteger[256] = {0};
        code = TA_CDLABANDONEDBABY(0, 127, Open, High, Low, Close, 0.2, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_default_cdl_advance_block") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int outInteger[256] = {0};
        code = TA_CDLADVANCEBLOCK(0, 15, Open, High, Low, Close, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_boundary_cdl_advance_block") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int boundaryEndIdx = TA_CDLADVANCEBLOCK_Lookback();
        if (boundaryEndIdx < 0) boundaryEndIdx = 0;
        if (boundaryEndIdx > 255) boundaryEndIdx = 255;
        int outInteger[256] = {0};
        code = TA_CDLADVANCEBLOCK(0, boundaryEndIdx, Open, High, Low, Close, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_seeded_cdl_advance_block") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        int outInteger[256] = {0};
        code = TA_CDLADVANCEBLOCK(0, 127, Open, High, Low, Close, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_variant_cdl_advance_block") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        int outInteger[256] = {0};
        code = TA_CDLADVANCEBLOCK(0, 127, Open, High, Low, Close, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_default_cdl_belt_hold") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int outInteger[256] = {0};
        code = TA_CDLBELTHOLD(0, 15, Open, High, Low, Close, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_boundary_cdl_belt_hold") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int boundaryEndIdx = TA_CDLBELTHOLD_Lookback();
        if (boundaryEndIdx < 0) boundaryEndIdx = 0;
        if (boundaryEndIdx > 255) boundaryEndIdx = 255;
        int outInteger[256] = {0};
        code = TA_CDLBELTHOLD(0, boundaryEndIdx, Open, High, Low, Close, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_seeded_cdl_belt_hold") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        int outInteger[256] = {0};
        code = TA_CDLBELTHOLD(0, 127, Open, High, Low, Close, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_variant_cdl_belt_hold") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        int outInteger[256] = {0};
        code = TA_CDLBELTHOLD(0, 127, Open, High, Low, Close, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_default_cdl_breakaway") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int outInteger[256] = {0};
        code = TA_CDLBREAKAWAY(0, 15, Open, High, Low, Close, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_boundary_cdl_breakaway") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int boundaryEndIdx = TA_CDLBREAKAWAY_Lookback();
        if (boundaryEndIdx < 0) boundaryEndIdx = 0;
        if (boundaryEndIdx > 255) boundaryEndIdx = 255;
        int outInteger[256] = {0};
        code = TA_CDLBREAKAWAY(0, boundaryEndIdx, Open, High, Low, Close, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_seeded_cdl_breakaway") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        int outInteger[256] = {0};
        code = TA_CDLBREAKAWAY(0, 127, Open, High, Low, Close, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_variant_cdl_breakaway") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        int outInteger[256] = {0};
        code = TA_CDLBREAKAWAY(0, 127, Open, High, Low, Close, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_default_cdl_closing_marubozu") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int outInteger[256] = {0};
        code = TA_CDLCLOSINGMARUBOZU(0, 15, Open, High, Low, Close, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_boundary_cdl_closing_marubozu") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int boundaryEndIdx = TA_CDLCLOSINGMARUBOZU_Lookback();
        if (boundaryEndIdx < 0) boundaryEndIdx = 0;
        if (boundaryEndIdx > 255) boundaryEndIdx = 255;
        int outInteger[256] = {0};
        code = TA_CDLCLOSINGMARUBOZU(0, boundaryEndIdx, Open, High, Low, Close, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_seeded_cdl_closing_marubozu") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        int outInteger[256] = {0};
        code = TA_CDLCLOSINGMARUBOZU(0, 127, Open, High, Low, Close, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_variant_cdl_closing_marubozu") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        int outInteger[256] = {0};
        code = TA_CDLCLOSINGMARUBOZU(0, 127, Open, High, Low, Close, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_default_cdl_conceal_babys_wall") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int outInteger[256] = {0};
        code = TA_CDLCONCEALBABYSWALL(0, 15, Open, High, Low, Close, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_boundary_cdl_conceal_babys_wall") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int boundaryEndIdx = TA_CDLCONCEALBABYSWALL_Lookback();
        if (boundaryEndIdx < 0) boundaryEndIdx = 0;
        if (boundaryEndIdx > 255) boundaryEndIdx = 255;
        int outInteger[256] = {0};
        code = TA_CDLCONCEALBABYSWALL(0, boundaryEndIdx, Open, High, Low, Close, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_seeded_cdl_conceal_babys_wall") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        int outInteger[256] = {0};
        code = TA_CDLCONCEALBABYSWALL(0, 127, Open, High, Low, Close, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_variant_cdl_conceal_babys_wall") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        int outInteger[256] = {0};
        code = TA_CDLCONCEALBABYSWALL(0, 127, Open, High, Low, Close, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_default_cdl_counter_attack") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int outInteger[256] = {0};
        code = TA_CDLCOUNTERATTACK(0, 15, Open, High, Low, Close, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_boundary_cdl_counter_attack") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int boundaryEndIdx = TA_CDLCOUNTERATTACK_Lookback();
        if (boundaryEndIdx < 0) boundaryEndIdx = 0;
        if (boundaryEndIdx > 255) boundaryEndIdx = 255;
        int outInteger[256] = {0};
        code = TA_CDLCOUNTERATTACK(0, boundaryEndIdx, Open, High, Low, Close, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_seeded_cdl_counter_attack") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        int outInteger[256] = {0};
        code = TA_CDLCOUNTERATTACK(0, 127, Open, High, Low, Close, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_variant_cdl_counter_attack") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        int outInteger[256] = {0};
        code = TA_CDLCOUNTERATTACK(0, 127, Open, High, Low, Close, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_default_cdl_dark_cloud_cover") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int outInteger[256] = {0};
        code = TA_CDLDARKCLOUDCOVER(0, 15, Open, High, Low, Close, 5.000000e-1, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_boundary_cdl_dark_cloud_cover") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int boundaryEndIdx = TA_CDLDARKCLOUDCOVER_Lookback(5.000000e-1);
        if (boundaryEndIdx < 0) boundaryEndIdx = 0;
        if (boundaryEndIdx > 255) boundaryEndIdx = 255;
        int outInteger[256] = {0};
        code = TA_CDLDARKCLOUDCOVER(0, boundaryEndIdx, Open, High, Low, Close, 5.000000e-1, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_seeded_cdl_dark_cloud_cover") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        int outInteger[256] = {0};
        code = TA_CDLDARKCLOUDCOVER(0, 127, Open, High, Low, Close, 5.000000e-1, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_variant_cdl_dark_cloud_cover") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        int outInteger[256] = {0};
        code = TA_CDLDARKCLOUDCOVER(0, 127, Open, High, Low, Close, 0.2, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_default_cdl_doji") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int outInteger[256] = {0};
        code = TA_CDLDOJI(0, 15, Open, High, Low, Close, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_boundary_cdl_doji") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int boundaryEndIdx = TA_CDLDOJI_Lookback();
        if (boundaryEndIdx < 0) boundaryEndIdx = 0;
        if (boundaryEndIdx > 255) boundaryEndIdx = 255;
        int outInteger[256] = {0};
        code = TA_CDLDOJI(0, boundaryEndIdx, Open, High, Low, Close, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_seeded_cdl_doji") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        int outInteger[256] = {0};
        code = TA_CDLDOJI(0, 127, Open, High, Low, Close, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_variant_cdl_doji") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        int outInteger[256] = {0};
        code = TA_CDLDOJI(0, 127, Open, High, Low, Close, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_default_cdl_doji_star") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int outInteger[256] = {0};
        code = TA_CDLDOJISTAR(0, 15, Open, High, Low, Close, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_boundary_cdl_doji_star") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int boundaryEndIdx = TA_CDLDOJISTAR_Lookback();
        if (boundaryEndIdx < 0) boundaryEndIdx = 0;
        if (boundaryEndIdx > 255) boundaryEndIdx = 255;
        int outInteger[256] = {0};
        code = TA_CDLDOJISTAR(0, boundaryEndIdx, Open, High, Low, Close, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_seeded_cdl_doji_star") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        int outInteger[256] = {0};
        code = TA_CDLDOJISTAR(0, 127, Open, High, Low, Close, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_variant_cdl_doji_star") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        int outInteger[256] = {0};
        code = TA_CDLDOJISTAR(0, 127, Open, High, Low, Close, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_default_cdl_dragonfly_doji") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int outInteger[256] = {0};
        code = TA_CDLDRAGONFLYDOJI(0, 15, Open, High, Low, Close, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_boundary_cdl_dragonfly_doji") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int boundaryEndIdx = TA_CDLDRAGONFLYDOJI_Lookback();
        if (boundaryEndIdx < 0) boundaryEndIdx = 0;
        if (boundaryEndIdx > 255) boundaryEndIdx = 255;
        int outInteger[256] = {0};
        code = TA_CDLDRAGONFLYDOJI(0, boundaryEndIdx, Open, High, Low, Close, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_seeded_cdl_dragonfly_doji") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        int outInteger[256] = {0};
        code = TA_CDLDRAGONFLYDOJI(0, 127, Open, High, Low, Close, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_variant_cdl_dragonfly_doji") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        int outInteger[256] = {0};
        code = TA_CDLDRAGONFLYDOJI(0, 127, Open, High, Low, Close, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_default_cdl_engulfing") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int outInteger[256] = {0};
        code = TA_CDLENGULFING(0, 15, Open, High, Low, Close, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_boundary_cdl_engulfing") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int boundaryEndIdx = TA_CDLENGULFING_Lookback();
        if (boundaryEndIdx < 0) boundaryEndIdx = 0;
        if (boundaryEndIdx > 255) boundaryEndIdx = 255;
        int outInteger[256] = {0};
        code = TA_CDLENGULFING(0, boundaryEndIdx, Open, High, Low, Close, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_seeded_cdl_engulfing") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        int outInteger[256] = {0};
        code = TA_CDLENGULFING(0, 127, Open, High, Low, Close, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_variant_cdl_engulfing") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        int outInteger[256] = {0};
        code = TA_CDLENGULFING(0, 127, Open, High, Low, Close, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_default_cdl_evening_doji_star") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int outInteger[256] = {0};
        code = TA_CDLEVENINGDOJISTAR(0, 15, Open, High, Low, Close, 3.000000e-1, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_boundary_cdl_evening_doji_star") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int boundaryEndIdx = TA_CDLEVENINGDOJISTAR_Lookback(3.000000e-1);
        if (boundaryEndIdx < 0) boundaryEndIdx = 0;
        if (boundaryEndIdx > 255) boundaryEndIdx = 255;
        int outInteger[256] = {0};
        code = TA_CDLEVENINGDOJISTAR(0, boundaryEndIdx, Open, High, Low, Close, 3.000000e-1, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_seeded_cdl_evening_doji_star") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        int outInteger[256] = {0};
        code = TA_CDLEVENINGDOJISTAR(0, 127, Open, High, Low, Close, 3.000000e-1, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_variant_cdl_evening_doji_star") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        int outInteger[256] = {0};
        code = TA_CDLEVENINGDOJISTAR(0, 127, Open, High, Low, Close, 0.2, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_default_cdl_evening_star") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int outInteger[256] = {0};
        code = TA_CDLEVENINGSTAR(0, 15, Open, High, Low, Close, 3.000000e-1, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_boundary_cdl_evening_star") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int boundaryEndIdx = TA_CDLEVENINGSTAR_Lookback(3.000000e-1);
        if (boundaryEndIdx < 0) boundaryEndIdx = 0;
        if (boundaryEndIdx > 255) boundaryEndIdx = 255;
        int outInteger[256] = {0};
        code = TA_CDLEVENINGSTAR(0, boundaryEndIdx, Open, High, Low, Close, 3.000000e-1, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_seeded_cdl_evening_star") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        int outInteger[256] = {0};
        code = TA_CDLEVENINGSTAR(0, 127, Open, High, Low, Close, 3.000000e-1, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_variant_cdl_evening_star") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        int outInteger[256] = {0};
        code = TA_CDLEVENINGSTAR(0, 127, Open, High, Low, Close, 0.2, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_default_cdl_gap_side_side_white") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int outInteger[256] = {0};
        code = TA_CDLGAPSIDESIDEWHITE(0, 15, Open, High, Low, Close, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_boundary_cdl_gap_side_side_white") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int boundaryEndIdx = TA_CDLGAPSIDESIDEWHITE_Lookback();
        if (boundaryEndIdx < 0) boundaryEndIdx = 0;
        if (boundaryEndIdx > 255) boundaryEndIdx = 255;
        int outInteger[256] = {0};
        code = TA_CDLGAPSIDESIDEWHITE(0, boundaryEndIdx, Open, High, Low, Close, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_seeded_cdl_gap_side_side_white") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        int outInteger[256] = {0};
        code = TA_CDLGAPSIDESIDEWHITE(0, 127, Open, High, Low, Close, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_variant_cdl_gap_side_side_white") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        int outInteger[256] = {0};
        code = TA_CDLGAPSIDESIDEWHITE(0, 127, Open, High, Low, Close, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_default_cdl_gravestone_doji") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int outInteger[256] = {0};
        code = TA_CDLGRAVESTONEDOJI(0, 15, Open, High, Low, Close, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_boundary_cdl_gravestone_doji") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int boundaryEndIdx = TA_CDLGRAVESTONEDOJI_Lookback();
        if (boundaryEndIdx < 0) boundaryEndIdx = 0;
        if (boundaryEndIdx > 255) boundaryEndIdx = 255;
        int outInteger[256] = {0};
        code = TA_CDLGRAVESTONEDOJI(0, boundaryEndIdx, Open, High, Low, Close, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_seeded_cdl_gravestone_doji") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        int outInteger[256] = {0};
        code = TA_CDLGRAVESTONEDOJI(0, 127, Open, High, Low, Close, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_variant_cdl_gravestone_doji") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        int outInteger[256] = {0};
        code = TA_CDLGRAVESTONEDOJI(0, 127, Open, High, Low, Close, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_default_cdl_hammer") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int outInteger[256] = {0};
        code = TA_CDLHAMMER(0, 15, Open, High, Low, Close, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_boundary_cdl_hammer") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int boundaryEndIdx = TA_CDLHAMMER_Lookback();
        if (boundaryEndIdx < 0) boundaryEndIdx = 0;
        if (boundaryEndIdx > 255) boundaryEndIdx = 255;
        int outInteger[256] = {0};
        code = TA_CDLHAMMER(0, boundaryEndIdx, Open, High, Low, Close, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_seeded_cdl_hammer") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        int outInteger[256] = {0};
        code = TA_CDLHAMMER(0, 127, Open, High, Low, Close, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_variant_cdl_hammer") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        int outInteger[256] = {0};
        code = TA_CDLHAMMER(0, 127, Open, High, Low, Close, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_default_cdl_hanging_man") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int outInteger[256] = {0};
        code = TA_CDLHANGINGMAN(0, 15, Open, High, Low, Close, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_boundary_cdl_hanging_man") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int boundaryEndIdx = TA_CDLHANGINGMAN_Lookback();
        if (boundaryEndIdx < 0) boundaryEndIdx = 0;
        if (boundaryEndIdx > 255) boundaryEndIdx = 255;
        int outInteger[256] = {0};
        code = TA_CDLHANGINGMAN(0, boundaryEndIdx, Open, High, Low, Close, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_seeded_cdl_hanging_man") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        int outInteger[256] = {0};
        code = TA_CDLHANGINGMAN(0, 127, Open, High, Low, Close, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_variant_cdl_hanging_man") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        int outInteger[256] = {0};
        code = TA_CDLHANGINGMAN(0, 127, Open, High, Low, Close, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_default_cdl_harami") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int outInteger[256] = {0};
        code = TA_CDLHARAMI(0, 15, Open, High, Low, Close, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_boundary_cdl_harami") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int boundaryEndIdx = TA_CDLHARAMI_Lookback();
        if (boundaryEndIdx < 0) boundaryEndIdx = 0;
        if (boundaryEndIdx > 255) boundaryEndIdx = 255;
        int outInteger[256] = {0};
        code = TA_CDLHARAMI(0, boundaryEndIdx, Open, High, Low, Close, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_seeded_cdl_harami") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        int outInteger[256] = {0};
        code = TA_CDLHARAMI(0, 127, Open, High, Low, Close, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_variant_cdl_harami") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        int outInteger[256] = {0};
        code = TA_CDLHARAMI(0, 127, Open, High, Low, Close, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_default_cdl_harami_cross") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int outInteger[256] = {0};
        code = TA_CDLHARAMICROSS(0, 15, Open, High, Low, Close, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_boundary_cdl_harami_cross") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int boundaryEndIdx = TA_CDLHARAMICROSS_Lookback();
        if (boundaryEndIdx < 0) boundaryEndIdx = 0;
        if (boundaryEndIdx > 255) boundaryEndIdx = 255;
        int outInteger[256] = {0};
        code = TA_CDLHARAMICROSS(0, boundaryEndIdx, Open, High, Low, Close, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_seeded_cdl_harami_cross") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        int outInteger[256] = {0};
        code = TA_CDLHARAMICROSS(0, 127, Open, High, Low, Close, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_variant_cdl_harami_cross") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        int outInteger[256] = {0};
        code = TA_CDLHARAMICROSS(0, 127, Open, High, Low, Close, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_default_cdl_high_wave") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int outInteger[256] = {0};
        code = TA_CDLHIGHWAVE(0, 15, Open, High, Low, Close, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_boundary_cdl_high_wave") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int boundaryEndIdx = TA_CDLHIGHWAVE_Lookback();
        if (boundaryEndIdx < 0) boundaryEndIdx = 0;
        if (boundaryEndIdx > 255) boundaryEndIdx = 255;
        int outInteger[256] = {0};
        code = TA_CDLHIGHWAVE(0, boundaryEndIdx, Open, High, Low, Close, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_seeded_cdl_high_wave") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        int outInteger[256] = {0};
        code = TA_CDLHIGHWAVE(0, 127, Open, High, Low, Close, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_variant_cdl_high_wave") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        int outInteger[256] = {0};
        code = TA_CDLHIGHWAVE(0, 127, Open, High, Low, Close, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_default_cdl_hikkake") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int outInteger[256] = {0};
        code = TA_CDLHIKKAKE(0, 15, Open, High, Low, Close, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_boundary_cdl_hikkake") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int boundaryEndIdx = TA_CDLHIKKAKE_Lookback();
        if (boundaryEndIdx < 0) boundaryEndIdx = 0;
        if (boundaryEndIdx > 255) boundaryEndIdx = 255;
        int outInteger[256] = {0};
        code = TA_CDLHIKKAKE(0, boundaryEndIdx, Open, High, Low, Close, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_seeded_cdl_hikkake") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        int outInteger[256] = {0};
        code = TA_CDLHIKKAKE(0, 127, Open, High, Low, Close, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_variant_cdl_hikkake") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        int outInteger[256] = {0};
        code = TA_CDLHIKKAKE(0, 127, Open, High, Low, Close, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_default_cdl_hikkake_mod") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int outInteger[256] = {0};
        code = TA_CDLHIKKAKEMOD(0, 15, Open, High, Low, Close, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_boundary_cdl_hikkake_mod") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int boundaryEndIdx = TA_CDLHIKKAKEMOD_Lookback();
        if (boundaryEndIdx < 0) boundaryEndIdx = 0;
        if (boundaryEndIdx > 255) boundaryEndIdx = 255;
        int outInteger[256] = {0};
        code = TA_CDLHIKKAKEMOD(0, boundaryEndIdx, Open, High, Low, Close, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_seeded_cdl_hikkake_mod") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        int outInteger[256] = {0};
        code = TA_CDLHIKKAKEMOD(0, 127, Open, High, Low, Close, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_variant_cdl_hikkake_mod") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        int outInteger[256] = {0};
        code = TA_CDLHIKKAKEMOD(0, 127, Open, High, Low, Close, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_default_cdl_homing_pigeon") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int outInteger[256] = {0};
        code = TA_CDLHOMINGPIGEON(0, 15, Open, High, Low, Close, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_boundary_cdl_homing_pigeon") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int boundaryEndIdx = TA_CDLHOMINGPIGEON_Lookback();
        if (boundaryEndIdx < 0) boundaryEndIdx = 0;
        if (boundaryEndIdx > 255) boundaryEndIdx = 255;
        int outInteger[256] = {0};
        code = TA_CDLHOMINGPIGEON(0, boundaryEndIdx, Open, High, Low, Close, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_seeded_cdl_homing_pigeon") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        int outInteger[256] = {0};
        code = TA_CDLHOMINGPIGEON(0, 127, Open, High, Low, Close, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_variant_cdl_homing_pigeon") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        int outInteger[256] = {0};
        code = TA_CDLHOMINGPIGEON(0, 127, Open, High, Low, Close, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_default_cdl_identical_3_crows") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int outInteger[256] = {0};
        code = TA_CDLIDENTICAL3CROWS(0, 15, Open, High, Low, Close, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_boundary_cdl_identical_3_crows") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int boundaryEndIdx = TA_CDLIDENTICAL3CROWS_Lookback();
        if (boundaryEndIdx < 0) boundaryEndIdx = 0;
        if (boundaryEndIdx > 255) boundaryEndIdx = 255;
        int outInteger[256] = {0};
        code = TA_CDLIDENTICAL3CROWS(0, boundaryEndIdx, Open, High, Low, Close, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_seeded_cdl_identical_3_crows") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        int outInteger[256] = {0};
        code = TA_CDLIDENTICAL3CROWS(0, 127, Open, High, Low, Close, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_variant_cdl_identical_3_crows") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        int outInteger[256] = {0};
        code = TA_CDLIDENTICAL3CROWS(0, 127, Open, High, Low, Close, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_default_cdl_in_neck") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int outInteger[256] = {0};
        code = TA_CDLINNECK(0, 15, Open, High, Low, Close, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_boundary_cdl_in_neck") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int boundaryEndIdx = TA_CDLINNECK_Lookback();
        if (boundaryEndIdx < 0) boundaryEndIdx = 0;
        if (boundaryEndIdx > 255) boundaryEndIdx = 255;
        int outInteger[256] = {0};
        code = TA_CDLINNECK(0, boundaryEndIdx, Open, High, Low, Close, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_seeded_cdl_in_neck") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        int outInteger[256] = {0};
        code = TA_CDLINNECK(0, 127, Open, High, Low, Close, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_variant_cdl_in_neck") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        int outInteger[256] = {0};
        code = TA_CDLINNECK(0, 127, Open, High, Low, Close, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_default_cdl_inverted_hammer") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int outInteger[256] = {0};
        code = TA_CDLINVERTEDHAMMER(0, 15, Open, High, Low, Close, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_boundary_cdl_inverted_hammer") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int boundaryEndIdx = TA_CDLINVERTEDHAMMER_Lookback();
        if (boundaryEndIdx < 0) boundaryEndIdx = 0;
        if (boundaryEndIdx > 255) boundaryEndIdx = 255;
        int outInteger[256] = {0};
        code = TA_CDLINVERTEDHAMMER(0, boundaryEndIdx, Open, High, Low, Close, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_seeded_cdl_inverted_hammer") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        int outInteger[256] = {0};
        code = TA_CDLINVERTEDHAMMER(0, 127, Open, High, Low, Close, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_variant_cdl_inverted_hammer") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        int outInteger[256] = {0};
        code = TA_CDLINVERTEDHAMMER(0, 127, Open, High, Low, Close, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_default_cdl_kicking") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int outInteger[256] = {0};
        code = TA_CDLKICKING(0, 15, Open, High, Low, Close, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_boundary_cdl_kicking") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int boundaryEndIdx = TA_CDLKICKING_Lookback();
        if (boundaryEndIdx < 0) boundaryEndIdx = 0;
        if (boundaryEndIdx > 255) boundaryEndIdx = 255;
        int outInteger[256] = {0};
        code = TA_CDLKICKING(0, boundaryEndIdx, Open, High, Low, Close, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_seeded_cdl_kicking") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        int outInteger[256] = {0};
        code = TA_CDLKICKING(0, 127, Open, High, Low, Close, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_variant_cdl_kicking") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        int outInteger[256] = {0};
        code = TA_CDLKICKING(0, 127, Open, High, Low, Close, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_default_cdl_kicking_by_length") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int outInteger[256] = {0};
        code = TA_CDLKICKINGBYLENGTH(0, 15, Open, High, Low, Close, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_boundary_cdl_kicking_by_length") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int boundaryEndIdx = TA_CDLKICKINGBYLENGTH_Lookback();
        if (boundaryEndIdx < 0) boundaryEndIdx = 0;
        if (boundaryEndIdx > 255) boundaryEndIdx = 255;
        int outInteger[256] = {0};
        code = TA_CDLKICKINGBYLENGTH(0, boundaryEndIdx, Open, High, Low, Close, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_seeded_cdl_kicking_by_length") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        int outInteger[256] = {0};
        code = TA_CDLKICKINGBYLENGTH(0, 127, Open, High, Low, Close, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_variant_cdl_kicking_by_length") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        int outInteger[256] = {0};
        code = TA_CDLKICKINGBYLENGTH(0, 127, Open, High, Low, Close, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_default_cdl_ladder_bottom") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int outInteger[256] = {0};
        code = TA_CDLLADDERBOTTOM(0, 15, Open, High, Low, Close, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_boundary_cdl_ladder_bottom") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int boundaryEndIdx = TA_CDLLADDERBOTTOM_Lookback();
        if (boundaryEndIdx < 0) boundaryEndIdx = 0;
        if (boundaryEndIdx > 255) boundaryEndIdx = 255;
        int outInteger[256] = {0};
        code = TA_CDLLADDERBOTTOM(0, boundaryEndIdx, Open, High, Low, Close, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_seeded_cdl_ladder_bottom") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        int outInteger[256] = {0};
        code = TA_CDLLADDERBOTTOM(0, 127, Open, High, Low, Close, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_variant_cdl_ladder_bottom") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        int outInteger[256] = {0};
        code = TA_CDLLADDERBOTTOM(0, 127, Open, High, Low, Close, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_default_cdl_long_legged_doji") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int outInteger[256] = {0};
        code = TA_CDLLONGLEGGEDDOJI(0, 15, Open, High, Low, Close, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_boundary_cdl_long_legged_doji") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int boundaryEndIdx = TA_CDLLONGLEGGEDDOJI_Lookback();
        if (boundaryEndIdx < 0) boundaryEndIdx = 0;
        if (boundaryEndIdx > 255) boundaryEndIdx = 255;
        int outInteger[256] = {0};
        code = TA_CDLLONGLEGGEDDOJI(0, boundaryEndIdx, Open, High, Low, Close, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_seeded_cdl_long_legged_doji") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        int outInteger[256] = {0};
        code = TA_CDLLONGLEGGEDDOJI(0, 127, Open, High, Low, Close, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_variant_cdl_long_legged_doji") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        int outInteger[256] = {0};
        code = TA_CDLLONGLEGGEDDOJI(0, 127, Open, High, Low, Close, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_default_cdl_long_line") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int outInteger[256] = {0};
        code = TA_CDLLONGLINE(0, 15, Open, High, Low, Close, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_boundary_cdl_long_line") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int boundaryEndIdx = TA_CDLLONGLINE_Lookback();
        if (boundaryEndIdx < 0) boundaryEndIdx = 0;
        if (boundaryEndIdx > 255) boundaryEndIdx = 255;
        int outInteger[256] = {0};
        code = TA_CDLLONGLINE(0, boundaryEndIdx, Open, High, Low, Close, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_seeded_cdl_long_line") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        int outInteger[256] = {0};
        code = TA_CDLLONGLINE(0, 127, Open, High, Low, Close, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_variant_cdl_long_line") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        int outInteger[256] = {0};
        code = TA_CDLLONGLINE(0, 127, Open, High, Low, Close, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_default_cdl_marubozu") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int outInteger[256] = {0};
        code = TA_CDLMARUBOZU(0, 15, Open, High, Low, Close, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_boundary_cdl_marubozu") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int boundaryEndIdx = TA_CDLMARUBOZU_Lookback();
        if (boundaryEndIdx < 0) boundaryEndIdx = 0;
        if (boundaryEndIdx > 255) boundaryEndIdx = 255;
        int outInteger[256] = {0};
        code = TA_CDLMARUBOZU(0, boundaryEndIdx, Open, High, Low, Close, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_seeded_cdl_marubozu") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        int outInteger[256] = {0};
        code = TA_CDLMARUBOZU(0, 127, Open, High, Low, Close, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_variant_cdl_marubozu") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        int outInteger[256] = {0};
        code = TA_CDLMARUBOZU(0, 127, Open, High, Low, Close, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_default_cdl_matching_low") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int outInteger[256] = {0};
        code = TA_CDLMATCHINGLOW(0, 15, Open, High, Low, Close, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_boundary_cdl_matching_low") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int boundaryEndIdx = TA_CDLMATCHINGLOW_Lookback();
        if (boundaryEndIdx < 0) boundaryEndIdx = 0;
        if (boundaryEndIdx > 255) boundaryEndIdx = 255;
        int outInteger[256] = {0};
        code = TA_CDLMATCHINGLOW(0, boundaryEndIdx, Open, High, Low, Close, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_seeded_cdl_matching_low") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        int outInteger[256] = {0};
        code = TA_CDLMATCHINGLOW(0, 127, Open, High, Low, Close, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_variant_cdl_matching_low") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        int outInteger[256] = {0};
        code = TA_CDLMATCHINGLOW(0, 127, Open, High, Low, Close, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_default_cdl_mat_hold") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int outInteger[256] = {0};
        code = TA_CDLMATHOLD(0, 15, Open, High, Low, Close, 5.000000e-1, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_boundary_cdl_mat_hold") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int boundaryEndIdx = TA_CDLMATHOLD_Lookback(5.000000e-1);
        if (boundaryEndIdx < 0) boundaryEndIdx = 0;
        if (boundaryEndIdx > 255) boundaryEndIdx = 255;
        int outInteger[256] = {0};
        code = TA_CDLMATHOLD(0, boundaryEndIdx, Open, High, Low, Close, 5.000000e-1, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_seeded_cdl_mat_hold") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        int outInteger[256] = {0};
        code = TA_CDLMATHOLD(0, 127, Open, High, Low, Close, 5.000000e-1, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_variant_cdl_mat_hold") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        int outInteger[256] = {0};
        code = TA_CDLMATHOLD(0, 127, Open, High, Low, Close, 0.2, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_default_cdl_morning_doji_star") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int outInteger[256] = {0};
        code = TA_CDLMORNINGDOJISTAR(0, 15, Open, High, Low, Close, 3.000000e-1, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_boundary_cdl_morning_doji_star") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int boundaryEndIdx = TA_CDLMORNINGDOJISTAR_Lookback(3.000000e-1);
        if (boundaryEndIdx < 0) boundaryEndIdx = 0;
        if (boundaryEndIdx > 255) boundaryEndIdx = 255;
        int outInteger[256] = {0};
        code = TA_CDLMORNINGDOJISTAR(0, boundaryEndIdx, Open, High, Low, Close, 3.000000e-1, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_seeded_cdl_morning_doji_star") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        int outInteger[256] = {0};
        code = TA_CDLMORNINGDOJISTAR(0, 127, Open, High, Low, Close, 3.000000e-1, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_variant_cdl_morning_doji_star") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        int outInteger[256] = {0};
        code = TA_CDLMORNINGDOJISTAR(0, 127, Open, High, Low, Close, 0.2, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_default_cdl_morning_star") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int outInteger[256] = {0};
        code = TA_CDLMORNINGSTAR(0, 15, Open, High, Low, Close, 3.000000e-1, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_boundary_cdl_morning_star") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int boundaryEndIdx = TA_CDLMORNINGSTAR_Lookback(3.000000e-1);
        if (boundaryEndIdx < 0) boundaryEndIdx = 0;
        if (boundaryEndIdx > 255) boundaryEndIdx = 255;
        int outInteger[256] = {0};
        code = TA_CDLMORNINGSTAR(0, boundaryEndIdx, Open, High, Low, Close, 3.000000e-1, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_seeded_cdl_morning_star") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        int outInteger[256] = {0};
        code = TA_CDLMORNINGSTAR(0, 127, Open, High, Low, Close, 3.000000e-1, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_variant_cdl_morning_star") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        int outInteger[256] = {0};
        code = TA_CDLMORNINGSTAR(0, 127, Open, High, Low, Close, 0.2, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_default_cdl_on_neck") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int outInteger[256] = {0};
        code = TA_CDLONNECK(0, 15, Open, High, Low, Close, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_boundary_cdl_on_neck") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int boundaryEndIdx = TA_CDLONNECK_Lookback();
        if (boundaryEndIdx < 0) boundaryEndIdx = 0;
        if (boundaryEndIdx > 255) boundaryEndIdx = 255;
        int outInteger[256] = {0};
        code = TA_CDLONNECK(0, boundaryEndIdx, Open, High, Low, Close, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_seeded_cdl_on_neck") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        int outInteger[256] = {0};
        code = TA_CDLONNECK(0, 127, Open, High, Low, Close, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_variant_cdl_on_neck") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        int outInteger[256] = {0};
        code = TA_CDLONNECK(0, 127, Open, High, Low, Close, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_default_cdl_piercing") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int outInteger[256] = {0};
        code = TA_CDLPIERCING(0, 15, Open, High, Low, Close, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_boundary_cdl_piercing") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int boundaryEndIdx = TA_CDLPIERCING_Lookback();
        if (boundaryEndIdx < 0) boundaryEndIdx = 0;
        if (boundaryEndIdx > 255) boundaryEndIdx = 255;
        int outInteger[256] = {0};
        code = TA_CDLPIERCING(0, boundaryEndIdx, Open, High, Low, Close, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_seeded_cdl_piercing") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        int outInteger[256] = {0};
        code = TA_CDLPIERCING(0, 127, Open, High, Low, Close, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_variant_cdl_piercing") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        int outInteger[256] = {0};
        code = TA_CDLPIERCING(0, 127, Open, High, Low, Close, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_default_cdl_rickshaw_man") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int outInteger[256] = {0};
        code = TA_CDLRICKSHAWMAN(0, 15, Open, High, Low, Close, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_boundary_cdl_rickshaw_man") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int boundaryEndIdx = TA_CDLRICKSHAWMAN_Lookback();
        if (boundaryEndIdx < 0) boundaryEndIdx = 0;
        if (boundaryEndIdx > 255) boundaryEndIdx = 255;
        int outInteger[256] = {0};
        code = TA_CDLRICKSHAWMAN(0, boundaryEndIdx, Open, High, Low, Close, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_seeded_cdl_rickshaw_man") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        int outInteger[256] = {0};
        code = TA_CDLRICKSHAWMAN(0, 127, Open, High, Low, Close, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_variant_cdl_rickshaw_man") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        int outInteger[256] = {0};
        code = TA_CDLRICKSHAWMAN(0, 127, Open, High, Low, Close, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_default_cdl_rise_fall_3_methods") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int outInteger[256] = {0};
        code = TA_CDLRISEFALL3METHODS(0, 15, Open, High, Low, Close, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_boundary_cdl_rise_fall_3_methods") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int boundaryEndIdx = TA_CDLRISEFALL3METHODS_Lookback();
        if (boundaryEndIdx < 0) boundaryEndIdx = 0;
        if (boundaryEndIdx > 255) boundaryEndIdx = 255;
        int outInteger[256] = {0};
        code = TA_CDLRISEFALL3METHODS(0, boundaryEndIdx, Open, High, Low, Close, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_seeded_cdl_rise_fall_3_methods") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        int outInteger[256] = {0};
        code = TA_CDLRISEFALL3METHODS(0, 127, Open, High, Low, Close, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_variant_cdl_rise_fall_3_methods") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        int outInteger[256] = {0};
        code = TA_CDLRISEFALL3METHODS(0, 127, Open, High, Low, Close, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_default_cdl_separating_lines") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int outInteger[256] = {0};
        code = TA_CDLSEPARATINGLINES(0, 15, Open, High, Low, Close, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_boundary_cdl_separating_lines") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int boundaryEndIdx = TA_CDLSEPARATINGLINES_Lookback();
        if (boundaryEndIdx < 0) boundaryEndIdx = 0;
        if (boundaryEndIdx > 255) boundaryEndIdx = 255;
        int outInteger[256] = {0};
        code = TA_CDLSEPARATINGLINES(0, boundaryEndIdx, Open, High, Low, Close, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_seeded_cdl_separating_lines") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        int outInteger[256] = {0};
        code = TA_CDLSEPARATINGLINES(0, 127, Open, High, Low, Close, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_variant_cdl_separating_lines") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        int outInteger[256] = {0};
        code = TA_CDLSEPARATINGLINES(0, 127, Open, High, Low, Close, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_default_cdl_shooting_star") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int outInteger[256] = {0};
        code = TA_CDLSHOOTINGSTAR(0, 15, Open, High, Low, Close, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_boundary_cdl_shooting_star") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int boundaryEndIdx = TA_CDLSHOOTINGSTAR_Lookback();
        if (boundaryEndIdx < 0) boundaryEndIdx = 0;
        if (boundaryEndIdx > 255) boundaryEndIdx = 255;
        int outInteger[256] = {0};
        code = TA_CDLSHOOTINGSTAR(0, boundaryEndIdx, Open, High, Low, Close, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_seeded_cdl_shooting_star") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        int outInteger[256] = {0};
        code = TA_CDLSHOOTINGSTAR(0, 127, Open, High, Low, Close, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_variant_cdl_shooting_star") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        int outInteger[256] = {0};
        code = TA_CDLSHOOTINGSTAR(0, 127, Open, High, Low, Close, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_default_cdl_short_line") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int outInteger[256] = {0};
        code = TA_CDLSHORTLINE(0, 15, Open, High, Low, Close, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_boundary_cdl_short_line") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int boundaryEndIdx = TA_CDLSHORTLINE_Lookback();
        if (boundaryEndIdx < 0) boundaryEndIdx = 0;
        if (boundaryEndIdx > 255) boundaryEndIdx = 255;
        int outInteger[256] = {0};
        code = TA_CDLSHORTLINE(0, boundaryEndIdx, Open, High, Low, Close, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_seeded_cdl_short_line") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        int outInteger[256] = {0};
        code = TA_CDLSHORTLINE(0, 127, Open, High, Low, Close, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_variant_cdl_short_line") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        int outInteger[256] = {0};
        code = TA_CDLSHORTLINE(0, 127, Open, High, Low, Close, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_default_cdl_spinning_top") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int outInteger[256] = {0};
        code = TA_CDLSPINNINGTOP(0, 15, Open, High, Low, Close, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_boundary_cdl_spinning_top") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int boundaryEndIdx = TA_CDLSPINNINGTOP_Lookback();
        if (boundaryEndIdx < 0) boundaryEndIdx = 0;
        if (boundaryEndIdx > 255) boundaryEndIdx = 255;
        int outInteger[256] = {0};
        code = TA_CDLSPINNINGTOP(0, boundaryEndIdx, Open, High, Low, Close, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_seeded_cdl_spinning_top") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        int outInteger[256] = {0};
        code = TA_CDLSPINNINGTOP(0, 127, Open, High, Low, Close, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_variant_cdl_spinning_top") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        int outInteger[256] = {0};
        code = TA_CDLSPINNINGTOP(0, 127, Open, High, Low, Close, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_default_cdl_stalled_pattern") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int outInteger[256] = {0};
        code = TA_CDLSTALLEDPATTERN(0, 15, Open, High, Low, Close, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_boundary_cdl_stalled_pattern") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int boundaryEndIdx = TA_CDLSTALLEDPATTERN_Lookback();
        if (boundaryEndIdx < 0) boundaryEndIdx = 0;
        if (boundaryEndIdx > 255) boundaryEndIdx = 255;
        int outInteger[256] = {0};
        code = TA_CDLSTALLEDPATTERN(0, boundaryEndIdx, Open, High, Low, Close, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_seeded_cdl_stalled_pattern") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        int outInteger[256] = {0};
        code = TA_CDLSTALLEDPATTERN(0, 127, Open, High, Low, Close, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_variant_cdl_stalled_pattern") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        int outInteger[256] = {0};
        code = TA_CDLSTALLEDPATTERN(0, 127, Open, High, Low, Close, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_default_cdl_stick_sandwich") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int outInteger[256] = {0};
        code = TA_CDLSTICKSANDWICH(0, 15, Open, High, Low, Close, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_boundary_cdl_stick_sandwich") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int boundaryEndIdx = TA_CDLSTICKSANDWICH_Lookback();
        if (boundaryEndIdx < 0) boundaryEndIdx = 0;
        if (boundaryEndIdx > 255) boundaryEndIdx = 255;
        int outInteger[256] = {0};
        code = TA_CDLSTICKSANDWICH(0, boundaryEndIdx, Open, High, Low, Close, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_seeded_cdl_stick_sandwich") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        int outInteger[256] = {0};
        code = TA_CDLSTICKSANDWICH(0, 127, Open, High, Low, Close, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_variant_cdl_stick_sandwich") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        int outInteger[256] = {0};
        code = TA_CDLSTICKSANDWICH(0, 127, Open, High, Low, Close, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_default_cdl_takuri") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int outInteger[256] = {0};
        code = TA_CDLTAKURI(0, 15, Open, High, Low, Close, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_boundary_cdl_takuri") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int boundaryEndIdx = TA_CDLTAKURI_Lookback();
        if (boundaryEndIdx < 0) boundaryEndIdx = 0;
        if (boundaryEndIdx > 255) boundaryEndIdx = 255;
        int outInteger[256] = {0};
        code = TA_CDLTAKURI(0, boundaryEndIdx, Open, High, Low, Close, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_seeded_cdl_takuri") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        int outInteger[256] = {0};
        code = TA_CDLTAKURI(0, 127, Open, High, Low, Close, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_variant_cdl_takuri") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        int outInteger[256] = {0};
        code = TA_CDLTAKURI(0, 127, Open, High, Low, Close, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_default_cdl_tasuki_gap") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int outInteger[256] = {0};
        code = TA_CDLTASUKIGAP(0, 15, Open, High, Low, Close, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_boundary_cdl_tasuki_gap") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int boundaryEndIdx = TA_CDLTASUKIGAP_Lookback();
        if (boundaryEndIdx < 0) boundaryEndIdx = 0;
        if (boundaryEndIdx > 255) boundaryEndIdx = 255;
        int outInteger[256] = {0};
        code = TA_CDLTASUKIGAP(0, boundaryEndIdx, Open, High, Low, Close, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_seeded_cdl_tasuki_gap") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        int outInteger[256] = {0};
        code = TA_CDLTASUKIGAP(0, 127, Open, High, Low, Close, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_variant_cdl_tasuki_gap") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        int outInteger[256] = {0};
        code = TA_CDLTASUKIGAP(0, 127, Open, High, Low, Close, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_default_cdl_thrusting") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int outInteger[256] = {0};
        code = TA_CDLTHRUSTING(0, 15, Open, High, Low, Close, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_boundary_cdl_thrusting") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int boundaryEndIdx = TA_CDLTHRUSTING_Lookback();
        if (boundaryEndIdx < 0) boundaryEndIdx = 0;
        if (boundaryEndIdx > 255) boundaryEndIdx = 255;
        int outInteger[256] = {0};
        code = TA_CDLTHRUSTING(0, boundaryEndIdx, Open, High, Low, Close, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_seeded_cdl_thrusting") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        int outInteger[256] = {0};
        code = TA_CDLTHRUSTING(0, 127, Open, High, Low, Close, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_variant_cdl_thrusting") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        int outInteger[256] = {0};
        code = TA_CDLTHRUSTING(0, 127, Open, High, Low, Close, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_default_cdl_tristar") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int outInteger[256] = {0};
        code = TA_CDLTRISTAR(0, 15, Open, High, Low, Close, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_boundary_cdl_tristar") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int boundaryEndIdx = TA_CDLTRISTAR_Lookback();
        if (boundaryEndIdx < 0) boundaryEndIdx = 0;
        if (boundaryEndIdx > 255) boundaryEndIdx = 255;
        int outInteger[256] = {0};
        code = TA_CDLTRISTAR(0, boundaryEndIdx, Open, High, Low, Close, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_seeded_cdl_tristar") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        int outInteger[256] = {0};
        code = TA_CDLTRISTAR(0, 127, Open, High, Low, Close, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_variant_cdl_tristar") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        int outInteger[256] = {0};
        code = TA_CDLTRISTAR(0, 127, Open, High, Low, Close, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_default_cdl_unique_3_river") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int outInteger[256] = {0};
        code = TA_CDLUNIQUE3RIVER(0, 15, Open, High, Low, Close, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_boundary_cdl_unique_3_river") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int boundaryEndIdx = TA_CDLUNIQUE3RIVER_Lookback();
        if (boundaryEndIdx < 0) boundaryEndIdx = 0;
        if (boundaryEndIdx > 255) boundaryEndIdx = 255;
        int outInteger[256] = {0};
        code = TA_CDLUNIQUE3RIVER(0, boundaryEndIdx, Open, High, Low, Close, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_seeded_cdl_unique_3_river") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        int outInteger[256] = {0};
        code = TA_CDLUNIQUE3RIVER(0, 127, Open, High, Low, Close, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_variant_cdl_unique_3_river") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        int outInteger[256] = {0};
        code = TA_CDLUNIQUE3RIVER(0, 127, Open, High, Low, Close, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_default_cdl_upside_gap_2_crows") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int outInteger[256] = {0};
        code = TA_CDLUPSIDEGAP2CROWS(0, 15, Open, High, Low, Close, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_boundary_cdl_upside_gap_2_crows") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int boundaryEndIdx = TA_CDLUPSIDEGAP2CROWS_Lookback();
        if (boundaryEndIdx < 0) boundaryEndIdx = 0;
        if (boundaryEndIdx > 255) boundaryEndIdx = 255;
        int outInteger[256] = {0};
        code = TA_CDLUPSIDEGAP2CROWS(0, boundaryEndIdx, Open, High, Low, Close, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_seeded_cdl_upside_gap_2_crows") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        int outInteger[256] = {0};
        code = TA_CDLUPSIDEGAP2CROWS(0, 127, Open, High, Low, Close, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_variant_cdl_upside_gap_2_crows") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        int outInteger[256] = {0};
        code = TA_CDLUPSIDEGAP2CROWS(0, 127, Open, High, Low, Close, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_default_cdl_x_side_gap_3_methods") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int outInteger[256] = {0};
        code = TA_CDLXSIDEGAP3METHODS(0, 15, Open, High, Low, Close, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_boundary_cdl_x_side_gap_3_methods") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int boundaryEndIdx = TA_CDLXSIDEGAP3METHODS_Lookback();
        if (boundaryEndIdx < 0) boundaryEndIdx = 0;
        if (boundaryEndIdx > 255) boundaryEndIdx = 255;
        int outInteger[256] = {0};
        code = TA_CDLXSIDEGAP3METHODS(0, boundaryEndIdx, Open, High, Low, Close, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_seeded_cdl_x_side_gap_3_methods") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        int outInteger[256] = {0};
        code = TA_CDLXSIDEGAP3METHODS(0, 127, Open, High, Low, Close, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_variant_cdl_x_side_gap_3_methods") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        int outInteger[256] = {0};
        code = TA_CDLXSIDEGAP3METHODS(0, 127, Open, High, Low, Close, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_default_ceil") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        double outReal[256] = {0.0};
        code = TA_CEIL(0, 15, inReal, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_boundary_ceil") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int boundaryEndIdx = TA_CEIL_Lookback();
        if (boundaryEndIdx < 0) boundaryEndIdx = 0;
        if (boundaryEndIdx > 255) boundaryEndIdx = 255;
        double outReal[256] = {0.0};
        code = TA_CEIL(0, boundaryEndIdx, inReal, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_seeded_ceil") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        double outReal[256] = {0.0};
        code = TA_CEIL(0, 127, inReal, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_variant_ceil") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        double outReal[256] = {0.0};
        code = TA_CEIL(0, 127, inReal, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_default_cmo") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        double outReal[256] = {0.0};
        code = TA_CMO(0, 15, inReal, 14, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_boundary_cmo") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int boundaryEndIdx = TA_CMO_Lookback(14);
        if (boundaryEndIdx < 0) boundaryEndIdx = 0;
        if (boundaryEndIdx > 255) boundaryEndIdx = 255;
        double outReal[256] = {0.0};
        code = TA_CMO(0, boundaryEndIdx, inReal, 14, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_seeded_cmo") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        double outReal[256] = {0.0};
        code = TA_CMO(0, 127, inReal, 14, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_variant_cmo") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        double outReal[256] = {0.0};
        code = TA_CMO(0, 127, inReal, 5, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_default_correl") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        double outReal[256] = {0.0};
        code = TA_CORREL(0, 15, inReal0, inReal1, 30, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_boundary_correl") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int boundaryEndIdx = TA_CORREL_Lookback(30);
        if (boundaryEndIdx < 0) boundaryEndIdx = 0;
        if (boundaryEndIdx > 255) boundaryEndIdx = 255;
        double outReal[256] = {0.0};
        code = TA_CORREL(0, boundaryEndIdx, inReal0, inReal1, 30, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_seeded_correl") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        double outReal[256] = {0.0};
        code = TA_CORREL(0, 127, inReal0, inReal1, 30, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_variant_correl") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        double outReal[256] = {0.0};
        code = TA_CORREL(0, 127, inReal0, inReal1, 5, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_default_cos") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        double outReal[256] = {0.0};
        code = TA_COS(0, 15, inReal, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_boundary_cos") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int boundaryEndIdx = TA_COS_Lookback();
        if (boundaryEndIdx < 0) boundaryEndIdx = 0;
        if (boundaryEndIdx > 255) boundaryEndIdx = 255;
        double outReal[256] = {0.0};
        code = TA_COS(0, boundaryEndIdx, inReal, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_seeded_cos") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        double outReal[256] = {0.0};
        code = TA_COS(0, 127, inReal, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_variant_cos") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        double outReal[256] = {0.0};
        code = TA_COS(0, 127, inReal, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_default_cosh") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        double outReal[256] = {0.0};
        code = TA_COSH(0, 15, inReal, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_boundary_cosh") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int boundaryEndIdx = TA_COSH_Lookback();
        if (boundaryEndIdx < 0) boundaryEndIdx = 0;
        if (boundaryEndIdx > 255) boundaryEndIdx = 255;
        double outReal[256] = {0.0};
        code = TA_COSH(0, boundaryEndIdx, inReal, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_seeded_cosh") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        double outReal[256] = {0.0};
        code = TA_COSH(0, 127, inReal, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_variant_cosh") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        double outReal[256] = {0.0};
        code = TA_COSH(0, 127, inReal, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_default_dema") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        double outReal[256] = {0.0};
        code = TA_DEMA(0, 15, inReal, 30, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_boundary_dema") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int boundaryEndIdx = TA_DEMA_Lookback(30);
        if (boundaryEndIdx < 0) boundaryEndIdx = 0;
        if (boundaryEndIdx > 255) boundaryEndIdx = 255;
        double outReal[256] = {0.0};
        code = TA_DEMA(0, boundaryEndIdx, inReal, 30, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_seeded_dema") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        double outReal[256] = {0.0};
        code = TA_DEMA(0, 127, inReal, 30, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_variant_dema") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        double outReal[256] = {0.0};
        code = TA_DEMA(0, 127, inReal, 5, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_default_div") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        double outReal[256] = {0.0};
        code = TA_DIV(0, 15, inReal0, inReal1, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_boundary_div") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int boundaryEndIdx = TA_DIV_Lookback();
        if (boundaryEndIdx < 0) boundaryEndIdx = 0;
        if (boundaryEndIdx > 255) boundaryEndIdx = 255;
        double outReal[256] = {0.0};
        code = TA_DIV(0, boundaryEndIdx, inReal0, inReal1, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_seeded_div") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        double outReal[256] = {0.0};
        code = TA_DIV(0, 127, inReal0, inReal1, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_variant_div") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        double outReal[256] = {0.0};
        code = TA_DIV(0, 127, inReal0, inReal1, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_default_dx") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        double outReal[256] = {0.0};
        code = TA_DX(0, 15, High, Low, Close, 14, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_boundary_dx") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int boundaryEndIdx = TA_DX_Lookback(14);
        if (boundaryEndIdx < 0) boundaryEndIdx = 0;
        if (boundaryEndIdx > 255) boundaryEndIdx = 255;
        double outReal[256] = {0.0};
        code = TA_DX(0, boundaryEndIdx, High, Low, Close, 14, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_seeded_dx") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        double outReal[256] = {0.0};
        code = TA_DX(0, 127, High, Low, Close, 14, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_variant_dx") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        double outReal[256] = {0.0};
        code = TA_DX(0, 127, High, Low, Close, 5, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_default_ema") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        double outReal[256] = {0.0};
        code = TA_EMA(0, 15, inReal, 30, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_boundary_ema") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int boundaryEndIdx = TA_EMA_Lookback(30);
        if (boundaryEndIdx < 0) boundaryEndIdx = 0;
        if (boundaryEndIdx > 255) boundaryEndIdx = 255;
        double outReal[256] = {0.0};
        code = TA_EMA(0, boundaryEndIdx, inReal, 30, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_seeded_ema") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        double outReal[256] = {0.0};
        code = TA_EMA(0, 127, inReal, 30, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_variant_ema") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        double outReal[256] = {0.0};
        code = TA_EMA(0, 127, inReal, 5, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_default_exp") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        double outReal[256] = {0.0};
        code = TA_EXP(0, 15, inReal, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_boundary_exp") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int boundaryEndIdx = TA_EXP_Lookback();
        if (boundaryEndIdx < 0) boundaryEndIdx = 0;
        if (boundaryEndIdx > 255) boundaryEndIdx = 255;
        double outReal[256] = {0.0};
        code = TA_EXP(0, boundaryEndIdx, inReal, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_seeded_exp") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        double outReal[256] = {0.0};
        code = TA_EXP(0, 127, inReal, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_variant_exp") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        double outReal[256] = {0.0};
        code = TA_EXP(0, 127, inReal, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_default_floor") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        double outReal[256] = {0.0};
        code = TA_FLOOR(0, 15, inReal, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_boundary_floor") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int boundaryEndIdx = TA_FLOOR_Lookback();
        if (boundaryEndIdx < 0) boundaryEndIdx = 0;
        if (boundaryEndIdx > 255) boundaryEndIdx = 255;
        double outReal[256] = {0.0};
        code = TA_FLOOR(0, boundaryEndIdx, inReal, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_seeded_floor") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        double outReal[256] = {0.0};
        code = TA_FLOOR(0, 127, inReal, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_variant_floor") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        double outReal[256] = {0.0};
        code = TA_FLOOR(0, 127, inReal, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_default_ht_dc_period") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        double outReal[256] = {0.0};
        code = TA_HT_DCPERIOD(0, 15, inReal, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_boundary_ht_dc_period") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int boundaryEndIdx = TA_HT_DCPERIOD_Lookback();
        if (boundaryEndIdx < 0) boundaryEndIdx = 0;
        if (boundaryEndIdx > 255) boundaryEndIdx = 255;
        double outReal[256] = {0.0};
        code = TA_HT_DCPERIOD(0, boundaryEndIdx, inReal, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_seeded_ht_dc_period") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        double outReal[256] = {0.0};
        code = TA_HT_DCPERIOD(0, 127, inReal, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_variant_ht_dc_period") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        double outReal[256] = {0.0};
        code = TA_HT_DCPERIOD(0, 127, inReal, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_default_ht_dc_phase") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        double outReal[256] = {0.0};
        code = TA_HT_DCPHASE(0, 15, inReal, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_boundary_ht_dc_phase") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int boundaryEndIdx = TA_HT_DCPHASE_Lookback();
        if (boundaryEndIdx < 0) boundaryEndIdx = 0;
        if (boundaryEndIdx > 255) boundaryEndIdx = 255;
        double outReal[256] = {0.0};
        code = TA_HT_DCPHASE(0, boundaryEndIdx, inReal, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_seeded_ht_dc_phase") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        double outReal[256] = {0.0};
        code = TA_HT_DCPHASE(0, 127, inReal, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_variant_ht_dc_phase") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        double outReal[256] = {0.0};
        code = TA_HT_DCPHASE(0, 127, inReal, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_default_ht_phasor") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        double outInPhase[256] = {0.0};
        double outQuadrature[256] = {0.0};
        code = TA_HT_PHASOR(0, 15, inReal, &outBegIdx, &outNbElement, outInPhase, outQuadrature);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %.17g", outInPhase[i]);
        }
        printf(" |");
        for (int i = 0; i < outNbElement; i++) {
            printf(" %.17g", outQuadrature[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_boundary_ht_phasor") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int boundaryEndIdx = TA_HT_PHASOR_Lookback();
        if (boundaryEndIdx < 0) boundaryEndIdx = 0;
        if (boundaryEndIdx > 255) boundaryEndIdx = 255;
        double outInPhase[256] = {0.0};
        double outQuadrature[256] = {0.0};
        code = TA_HT_PHASOR(0, boundaryEndIdx, inReal, &outBegIdx, &outNbElement, outInPhase, outQuadrature);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %.17g", outInPhase[i]);
        }
        printf(" |");
        for (int i = 0; i < outNbElement; i++) {
            printf(" %.17g", outQuadrature[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_seeded_ht_phasor") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        double outInPhase[256] = {0.0};
        double outQuadrature[256] = {0.0};
        code = TA_HT_PHASOR(0, 127, inReal, &outBegIdx, &outNbElement, outInPhase, outQuadrature);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %.17g", outInPhase[i]);
        }
        printf(" |");
        for (int i = 0; i < outNbElement; i++) {
            printf(" %.17g", outQuadrature[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_variant_ht_phasor") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        double outInPhase[256] = {0.0};
        double outQuadrature[256] = {0.0};
        code = TA_HT_PHASOR(0, 127, inReal, &outBegIdx, &outNbElement, outInPhase, outQuadrature);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %.17g", outInPhase[i]);
        }
        printf(" |");
        for (int i = 0; i < outNbElement; i++) {
            printf(" %.17g", outQuadrature[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_default_ht_sine") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        double outSine[256] = {0.0};
        double outLeadSine[256] = {0.0};
        code = TA_HT_SINE(0, 15, inReal, &outBegIdx, &outNbElement, outSine, outLeadSine);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %.17g", outSine[i]);
        }
        printf(" |");
        for (int i = 0; i < outNbElement; i++) {
            printf(" %.17g", outLeadSine[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_boundary_ht_sine") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int boundaryEndIdx = TA_HT_SINE_Lookback();
        if (boundaryEndIdx < 0) boundaryEndIdx = 0;
        if (boundaryEndIdx > 255) boundaryEndIdx = 255;
        double outSine[256] = {0.0};
        double outLeadSine[256] = {0.0};
        code = TA_HT_SINE(0, boundaryEndIdx, inReal, &outBegIdx, &outNbElement, outSine, outLeadSine);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %.17g", outSine[i]);
        }
        printf(" |");
        for (int i = 0; i < outNbElement; i++) {
            printf(" %.17g", outLeadSine[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_seeded_ht_sine") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        double outSine[256] = {0.0};
        double outLeadSine[256] = {0.0};
        code = TA_HT_SINE(0, 127, inReal, &outBegIdx, &outNbElement, outSine, outLeadSine);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %.17g", outSine[i]);
        }
        printf(" |");
        for (int i = 0; i < outNbElement; i++) {
            printf(" %.17g", outLeadSine[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_variant_ht_sine") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        double outSine[256] = {0.0};
        double outLeadSine[256] = {0.0};
        code = TA_HT_SINE(0, 127, inReal, &outBegIdx, &outNbElement, outSine, outLeadSine);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %.17g", outSine[i]);
        }
        printf(" |");
        for (int i = 0; i < outNbElement; i++) {
            printf(" %.17g", outLeadSine[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_default_ht_trendline") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        double outReal[256] = {0.0};
        code = TA_HT_TRENDLINE(0, 15, inReal, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_boundary_ht_trendline") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int boundaryEndIdx = TA_HT_TRENDLINE_Lookback();
        if (boundaryEndIdx < 0) boundaryEndIdx = 0;
        if (boundaryEndIdx > 255) boundaryEndIdx = 255;
        double outReal[256] = {0.0};
        code = TA_HT_TRENDLINE(0, boundaryEndIdx, inReal, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_seeded_ht_trendline") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        double outReal[256] = {0.0};
        code = TA_HT_TRENDLINE(0, 127, inReal, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_variant_ht_trendline") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        double outReal[256] = {0.0};
        code = TA_HT_TRENDLINE(0, 127, inReal, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_default_ht_trend_mode") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int outInteger[256] = {0};
        code = TA_HT_TRENDMODE(0, 15, inReal, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_boundary_ht_trend_mode") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int boundaryEndIdx = TA_HT_TRENDMODE_Lookback();
        if (boundaryEndIdx < 0) boundaryEndIdx = 0;
        if (boundaryEndIdx > 255) boundaryEndIdx = 255;
        int outInteger[256] = {0};
        code = TA_HT_TRENDMODE(0, boundaryEndIdx, inReal, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_seeded_ht_trend_mode") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        int outInteger[256] = {0};
        code = TA_HT_TRENDMODE(0, 127, inReal, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_variant_ht_trend_mode") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        int outInteger[256] = {0};
        code = TA_HT_TRENDMODE(0, 127, inReal, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_default_imi") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        double outReal[256] = {0.0};
        code = TA_IMI(0, 15, Open, Close, 14, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_boundary_imi") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int boundaryEndIdx = TA_IMI_Lookback(14);
        if (boundaryEndIdx < 0) boundaryEndIdx = 0;
        if (boundaryEndIdx > 255) boundaryEndIdx = 255;
        double outReal[256] = {0.0};
        code = TA_IMI(0, boundaryEndIdx, Open, Close, 14, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_seeded_imi") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        double outReal[256] = {0.0};
        code = TA_IMI(0, 127, Open, Close, 14, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_variant_imi") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        double outReal[256] = {0.0};
        code = TA_IMI(0, 127, Open, Close, 5, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_default_kama") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        double outReal[256] = {0.0};
        code = TA_KAMA(0, 15, inReal, 30, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_boundary_kama") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int boundaryEndIdx = TA_KAMA_Lookback(30);
        if (boundaryEndIdx < 0) boundaryEndIdx = 0;
        if (boundaryEndIdx > 255) boundaryEndIdx = 255;
        double outReal[256] = {0.0};
        code = TA_KAMA(0, boundaryEndIdx, inReal, 30, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_seeded_kama") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        double outReal[256] = {0.0};
        code = TA_KAMA(0, 127, inReal, 30, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_variant_kama") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        double outReal[256] = {0.0};
        code = TA_KAMA(0, 127, inReal, 5, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_default_linearreg") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        double outReal[256] = {0.0};
        code = TA_LINEARREG(0, 15, inReal, 14, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_boundary_linearreg") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int boundaryEndIdx = TA_LINEARREG_Lookback(14);
        if (boundaryEndIdx < 0) boundaryEndIdx = 0;
        if (boundaryEndIdx > 255) boundaryEndIdx = 255;
        double outReal[256] = {0.0};
        code = TA_LINEARREG(0, boundaryEndIdx, inReal, 14, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_seeded_linearreg") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        double outReal[256] = {0.0};
        code = TA_LINEARREG(0, 127, inReal, 14, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_variant_linearreg") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        double outReal[256] = {0.0};
        code = TA_LINEARREG(0, 127, inReal, 5, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_default_linearreg_angle") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        double outReal[256] = {0.0};
        code = TA_LINEARREG_ANGLE(0, 15, inReal, 14, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_boundary_linearreg_angle") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int boundaryEndIdx = TA_LINEARREG_ANGLE_Lookback(14);
        if (boundaryEndIdx < 0) boundaryEndIdx = 0;
        if (boundaryEndIdx > 255) boundaryEndIdx = 255;
        double outReal[256] = {0.0};
        code = TA_LINEARREG_ANGLE(0, boundaryEndIdx, inReal, 14, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_seeded_linearreg_angle") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        double outReal[256] = {0.0};
        code = TA_LINEARREG_ANGLE(0, 127, inReal, 14, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_variant_linearreg_angle") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        double outReal[256] = {0.0};
        code = TA_LINEARREG_ANGLE(0, 127, inReal, 5, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_default_linearreg_intercept") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        double outReal[256] = {0.0};
        code = TA_LINEARREG_INTERCEPT(0, 15, inReal, 14, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_boundary_linearreg_intercept") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int boundaryEndIdx = TA_LINEARREG_INTERCEPT_Lookback(14);
        if (boundaryEndIdx < 0) boundaryEndIdx = 0;
        if (boundaryEndIdx > 255) boundaryEndIdx = 255;
        double outReal[256] = {0.0};
        code = TA_LINEARREG_INTERCEPT(0, boundaryEndIdx, inReal, 14, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_seeded_linearreg_intercept") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        double outReal[256] = {0.0};
        code = TA_LINEARREG_INTERCEPT(0, 127, inReal, 14, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_variant_linearreg_intercept") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        double outReal[256] = {0.0};
        code = TA_LINEARREG_INTERCEPT(0, 127, inReal, 5, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_default_linearreg_slope") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        double outReal[256] = {0.0};
        code = TA_LINEARREG_SLOPE(0, 15, inReal, 14, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_boundary_linearreg_slope") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int boundaryEndIdx = TA_LINEARREG_SLOPE_Lookback(14);
        if (boundaryEndIdx < 0) boundaryEndIdx = 0;
        if (boundaryEndIdx > 255) boundaryEndIdx = 255;
        double outReal[256] = {0.0};
        code = TA_LINEARREG_SLOPE(0, boundaryEndIdx, inReal, 14, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_seeded_linearreg_slope") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        double outReal[256] = {0.0};
        code = TA_LINEARREG_SLOPE(0, 127, inReal, 14, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_variant_linearreg_slope") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        double outReal[256] = {0.0};
        code = TA_LINEARREG_SLOPE(0, 127, inReal, 5, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_default_ln") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        double outReal[256] = {0.0};
        code = TA_LN(0, 15, inReal, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_boundary_ln") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int boundaryEndIdx = TA_LN_Lookback();
        if (boundaryEndIdx < 0) boundaryEndIdx = 0;
        if (boundaryEndIdx > 255) boundaryEndIdx = 255;
        double outReal[256] = {0.0};
        code = TA_LN(0, boundaryEndIdx, inReal, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_seeded_ln") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        double outReal[256] = {0.0};
        code = TA_LN(0, 127, inReal, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_variant_ln") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        double outReal[256] = {0.0};
        code = TA_LN(0, 127, inReal, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_default_log10") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        double outReal[256] = {0.0};
        code = TA_LOG10(0, 15, inReal, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_boundary_log10") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int boundaryEndIdx = TA_LOG10_Lookback();
        if (boundaryEndIdx < 0) boundaryEndIdx = 0;
        if (boundaryEndIdx > 255) boundaryEndIdx = 255;
        double outReal[256] = {0.0};
        code = TA_LOG10(0, boundaryEndIdx, inReal, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_seeded_log10") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        double outReal[256] = {0.0};
        code = TA_LOG10(0, 127, inReal, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_variant_log10") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        double outReal[256] = {0.0};
        code = TA_LOG10(0, 127, inReal, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_default_ma") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        double outReal[256] = {0.0};
        code = TA_MA(0, 15, inReal, 30, 0, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_boundary_ma") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int boundaryEndIdx = TA_MA_Lookback(30, 0);
        if (boundaryEndIdx < 0) boundaryEndIdx = 0;
        if (boundaryEndIdx > 255) boundaryEndIdx = 255;
        double outReal[256] = {0.0};
        code = TA_MA(0, boundaryEndIdx, inReal, 30, 0, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_seeded_ma") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        double outReal[256] = {0.0};
        code = TA_MA(0, 127, inReal, 30, 0, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_variant_ma") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        double outReal[256] = {0.0};
        code = TA_MA(0, 127, inReal, 5, 1, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_default_macd") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        double outMACD[256] = {0.0};
        double outMACDSignal[256] = {0.0};
        double outMACDHist[256] = {0.0};
        code = TA_MACD(0, 15, inReal, 12, 26, 9, &outBegIdx, &outNbElement, outMACD, outMACDSignal, outMACDHist);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %.17g", outMACD[i]);
        }
        printf(" |");
        for (int i = 0; i < outNbElement; i++) {
            printf(" %.17g", outMACDSignal[i]);
        }
        printf(" |");
        for (int i = 0; i < outNbElement; i++) {
            printf(" %.17g", outMACDHist[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_boundary_macd") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int boundaryEndIdx = TA_MACD_Lookback(12, 26, 9);
        if (boundaryEndIdx < 0) boundaryEndIdx = 0;
        if (boundaryEndIdx > 255) boundaryEndIdx = 255;
        double outMACD[256] = {0.0};
        double outMACDSignal[256] = {0.0};
        double outMACDHist[256] = {0.0};
        code = TA_MACD(0, boundaryEndIdx, inReal, 12, 26, 9, &outBegIdx, &outNbElement, outMACD, outMACDSignal, outMACDHist);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %.17g", outMACD[i]);
        }
        printf(" |");
        for (int i = 0; i < outNbElement; i++) {
            printf(" %.17g", outMACDSignal[i]);
        }
        printf(" |");
        for (int i = 0; i < outNbElement; i++) {
            printf(" %.17g", outMACDHist[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_seeded_macd") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        double outMACD[256] = {0.0};
        double outMACDSignal[256] = {0.0};
        double outMACDHist[256] = {0.0};
        code = TA_MACD(0, 127, inReal, 12, 26, 9, &outBegIdx, &outNbElement, outMACD, outMACDSignal, outMACDHist);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %.17g", outMACD[i]);
        }
        printf(" |");
        for (int i = 0; i < outNbElement; i++) {
            printf(" %.17g", outMACDSignal[i]);
        }
        printf(" |");
        for (int i = 0; i < outNbElement; i++) {
            printf(" %.17g", outMACDHist[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_variant_macd") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        double outMACD[256] = {0.0};
        double outMACDSignal[256] = {0.0};
        double outMACDHist[256] = {0.0};
        code = TA_MACD(0, 127, inReal, 8, 17, 4, &outBegIdx, &outNbElement, outMACD, outMACDSignal, outMACDHist);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %.17g", outMACD[i]);
        }
        printf(" |");
        for (int i = 0; i < outNbElement; i++) {
            printf(" %.17g", outMACDSignal[i]);
        }
        printf(" |");
        for (int i = 0; i < outNbElement; i++) {
            printf(" %.17g", outMACDHist[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_default_macdext") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        double outMACD[256] = {0.0};
        double outMACDSignal[256] = {0.0};
        double outMACDHist[256] = {0.0};
        code = TA_MACDEXT(0, 15, inReal, 12, 0, 26, 0, 9, 0, &outBegIdx, &outNbElement, outMACD, outMACDSignal, outMACDHist);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %.17g", outMACD[i]);
        }
        printf(" |");
        for (int i = 0; i < outNbElement; i++) {
            printf(" %.17g", outMACDSignal[i]);
        }
        printf(" |");
        for (int i = 0; i < outNbElement; i++) {
            printf(" %.17g", outMACDHist[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_boundary_macdext") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int boundaryEndIdx = TA_MACDEXT_Lookback(12, 0, 26, 0, 9, 0);
        if (boundaryEndIdx < 0) boundaryEndIdx = 0;
        if (boundaryEndIdx > 255) boundaryEndIdx = 255;
        double outMACD[256] = {0.0};
        double outMACDSignal[256] = {0.0};
        double outMACDHist[256] = {0.0};
        code = TA_MACDEXT(0, boundaryEndIdx, inReal, 12, 0, 26, 0, 9, 0, &outBegIdx, &outNbElement, outMACD, outMACDSignal, outMACDHist);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %.17g", outMACD[i]);
        }
        printf(" |");
        for (int i = 0; i < outNbElement; i++) {
            printf(" %.17g", outMACDSignal[i]);
        }
        printf(" |");
        for (int i = 0; i < outNbElement; i++) {
            printf(" %.17g", outMACDHist[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_seeded_macdext") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        double outMACD[256] = {0.0};
        double outMACDSignal[256] = {0.0};
        double outMACDHist[256] = {0.0};
        code = TA_MACDEXT(0, 127, inReal, 12, 0, 26, 0, 9, 0, &outBegIdx, &outNbElement, outMACD, outMACDSignal, outMACDHist);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %.17g", outMACD[i]);
        }
        printf(" |");
        for (int i = 0; i < outNbElement; i++) {
            printf(" %.17g", outMACDSignal[i]);
        }
        printf(" |");
        for (int i = 0; i < outNbElement; i++) {
            printf(" %.17g", outMACDHist[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_variant_macdext") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        double outMACD[256] = {0.0};
        double outMACDSignal[256] = {0.0};
        double outMACDHist[256] = {0.0};
        code = TA_MACDEXT(0, 127, inReal, 8, 0, 17, 0, 4, 0, &outBegIdx, &outNbElement, outMACD, outMACDSignal, outMACDHist);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %.17g", outMACD[i]);
        }
        printf(" |");
        for (int i = 0; i < outNbElement; i++) {
            printf(" %.17g", outMACDSignal[i]);
        }
        printf(" |");
        for (int i = 0; i < outNbElement; i++) {
            printf(" %.17g", outMACDHist[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_default_macdfix") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        double outMACD[256] = {0.0};
        double outMACDSignal[256] = {0.0};
        double outMACDHist[256] = {0.0};
        code = TA_MACDFIX(0, 15, inReal, 9, &outBegIdx, &outNbElement, outMACD, outMACDSignal, outMACDHist);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %.17g", outMACD[i]);
        }
        printf(" |");
        for (int i = 0; i < outNbElement; i++) {
            printf(" %.17g", outMACDSignal[i]);
        }
        printf(" |");
        for (int i = 0; i < outNbElement; i++) {
            printf(" %.17g", outMACDHist[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_boundary_macdfix") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int boundaryEndIdx = TA_MACDFIX_Lookback(9);
        if (boundaryEndIdx < 0) boundaryEndIdx = 0;
        if (boundaryEndIdx > 255) boundaryEndIdx = 255;
        double outMACD[256] = {0.0};
        double outMACDSignal[256] = {0.0};
        double outMACDHist[256] = {0.0};
        code = TA_MACDFIX(0, boundaryEndIdx, inReal, 9, &outBegIdx, &outNbElement, outMACD, outMACDSignal, outMACDHist);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %.17g", outMACD[i]);
        }
        printf(" |");
        for (int i = 0; i < outNbElement; i++) {
            printf(" %.17g", outMACDSignal[i]);
        }
        printf(" |");
        for (int i = 0; i < outNbElement; i++) {
            printf(" %.17g", outMACDHist[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_seeded_macdfix") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        double outMACD[256] = {0.0};
        double outMACDSignal[256] = {0.0};
        double outMACDHist[256] = {0.0};
        code = TA_MACDFIX(0, 127, inReal, 9, &outBegIdx, &outNbElement, outMACD, outMACDSignal, outMACDHist);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %.17g", outMACD[i]);
        }
        printf(" |");
        for (int i = 0; i < outNbElement; i++) {
            printf(" %.17g", outMACDSignal[i]);
        }
        printf(" |");
        for (int i = 0; i < outNbElement; i++) {
            printf(" %.17g", outMACDHist[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_variant_macdfix") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        double outMACD[256] = {0.0};
        double outMACDSignal[256] = {0.0};
        double outMACDHist[256] = {0.0};
        code = TA_MACDFIX(0, 127, inReal, 4, &outBegIdx, &outNbElement, outMACD, outMACDSignal, outMACDHist);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %.17g", outMACD[i]);
        }
        printf(" |");
        for (int i = 0; i < outNbElement; i++) {
            printf(" %.17g", outMACDSignal[i]);
        }
        printf(" |");
        for (int i = 0; i < outNbElement; i++) {
            printf(" %.17g", outMACDHist[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_default_mama") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        double outMAMA[256] = {0.0};
        double outFAMA[256] = {0.0};
        code = TA_MAMA(0, 15, inReal, 5.000000e-1, 5.000000e-2, &outBegIdx, &outNbElement, outMAMA, outFAMA);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %.17g", outMAMA[i]);
        }
        printf(" |");
        for (int i = 0; i < outNbElement; i++) {
            printf(" %.17g", outFAMA[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_boundary_mama") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int boundaryEndIdx = TA_MAMA_Lookback(5.000000e-1, 5.000000e-2);
        if (boundaryEndIdx < 0) boundaryEndIdx = 0;
        if (boundaryEndIdx > 255) boundaryEndIdx = 255;
        double outMAMA[256] = {0.0};
        double outFAMA[256] = {0.0};
        code = TA_MAMA(0, boundaryEndIdx, inReal, 5.000000e-1, 5.000000e-2, &outBegIdx, &outNbElement, outMAMA, outFAMA);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %.17g", outMAMA[i]);
        }
        printf(" |");
        for (int i = 0; i < outNbElement; i++) {
            printf(" %.17g", outFAMA[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_seeded_mama") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        double outMAMA[256] = {0.0};
        double outFAMA[256] = {0.0};
        code = TA_MAMA(0, 127, inReal, 5.000000e-1, 5.000000e-2, &outBegIdx, &outNbElement, outMAMA, outFAMA);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %.17g", outMAMA[i]);
        }
        printf(" |");
        for (int i = 0; i < outNbElement; i++) {
            printf(" %.17g", outFAMA[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_variant_mama") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        double outMAMA[256] = {0.0};
        double outFAMA[256] = {0.0};
        code = TA_MAMA(0, 127, inReal, 0.8, 0.1, &outBegIdx, &outNbElement, outMAMA, outFAMA);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %.17g", outMAMA[i]);
        }
        printf(" |");
        for (int i = 0; i < outNbElement; i++) {
            printf(" %.17g", outFAMA[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_default_mavp") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        double outReal[256] = {0.0};
        code = TA_MAVP(0, 15, inReal, inPeriods, 2, 30, 0, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_boundary_mavp") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int boundaryEndIdx = TA_MAVP_Lookback(2, 30, 0);
        if (boundaryEndIdx < 0) boundaryEndIdx = 0;
        if (boundaryEndIdx > 255) boundaryEndIdx = 255;
        double outReal[256] = {0.0};
        code = TA_MAVP(0, boundaryEndIdx, inReal, inPeriods, 2, 30, 0, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_seeded_mavp") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        double outReal[256] = {0.0};
        code = TA_MAVP(0, 127, inReal, inPeriods, 2, 30, 0, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_variant_mavp") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        double outReal[256] = {0.0};
        code = TA_MAVP(0, 127, inReal, inPeriods, 3, 9, 1, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_default_max") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        double outReal[256] = {0.0};
        code = TA_MAX(0, 15, inReal, 30, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_boundary_max") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int boundaryEndIdx = TA_MAX_Lookback(30);
        if (boundaryEndIdx < 0) boundaryEndIdx = 0;
        if (boundaryEndIdx > 255) boundaryEndIdx = 255;
        double outReal[256] = {0.0};
        code = TA_MAX(0, boundaryEndIdx, inReal, 30, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_seeded_max") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        double outReal[256] = {0.0};
        code = TA_MAX(0, 127, inReal, 30, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_variant_max") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        double outReal[256] = {0.0};
        code = TA_MAX(0, 127, inReal, 5, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_default_max_index") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int outInteger[256] = {0};
        code = TA_MAXINDEX(0, 15, inReal, 30, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_boundary_max_index") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int boundaryEndIdx = TA_MAXINDEX_Lookback(30);
        if (boundaryEndIdx < 0) boundaryEndIdx = 0;
        if (boundaryEndIdx > 255) boundaryEndIdx = 255;
        int outInteger[256] = {0};
        code = TA_MAXINDEX(0, boundaryEndIdx, inReal, 30, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_seeded_max_index") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        int outInteger[256] = {0};
        code = TA_MAXINDEX(0, 127, inReal, 30, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_variant_max_index") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        int outInteger[256] = {0};
        code = TA_MAXINDEX(0, 127, inReal, 5, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_default_med_price") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        double outReal[256] = {0.0};
        code = TA_MEDPRICE(0, 15, High, Low, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_boundary_med_price") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int boundaryEndIdx = TA_MEDPRICE_Lookback();
        if (boundaryEndIdx < 0) boundaryEndIdx = 0;
        if (boundaryEndIdx > 255) boundaryEndIdx = 255;
        double outReal[256] = {0.0};
        code = TA_MEDPRICE(0, boundaryEndIdx, High, Low, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_seeded_med_price") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        double outReal[256] = {0.0};
        code = TA_MEDPRICE(0, 127, High, Low, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_variant_med_price") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        double outReal[256] = {0.0};
        code = TA_MEDPRICE(0, 127, High, Low, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_default_mfi") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        double outReal[256] = {0.0};
        code = TA_MFI(0, 15, High, Low, Close, Volume, 14, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_boundary_mfi") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int boundaryEndIdx = TA_MFI_Lookback(14);
        if (boundaryEndIdx < 0) boundaryEndIdx = 0;
        if (boundaryEndIdx > 255) boundaryEndIdx = 255;
        double outReal[256] = {0.0};
        code = TA_MFI(0, boundaryEndIdx, High, Low, Close, Volume, 14, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_seeded_mfi") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        double outReal[256] = {0.0};
        code = TA_MFI(0, 127, High, Low, Close, Volume, 14, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_variant_mfi") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        double outReal[256] = {0.0};
        code = TA_MFI(0, 127, High, Low, Close, Volume, 5, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_default_mid_point") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        double outReal[256] = {0.0};
        code = TA_MIDPOINT(0, 15, inReal, 14, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_boundary_mid_point") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int boundaryEndIdx = TA_MIDPOINT_Lookback(14);
        if (boundaryEndIdx < 0) boundaryEndIdx = 0;
        if (boundaryEndIdx > 255) boundaryEndIdx = 255;
        double outReal[256] = {0.0};
        code = TA_MIDPOINT(0, boundaryEndIdx, inReal, 14, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_seeded_mid_point") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        double outReal[256] = {0.0};
        code = TA_MIDPOINT(0, 127, inReal, 14, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_variant_mid_point") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        double outReal[256] = {0.0};
        code = TA_MIDPOINT(0, 127, inReal, 5, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_default_mid_price") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        double outReal[256] = {0.0};
        code = TA_MIDPRICE(0, 15, High, Low, 14, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_boundary_mid_price") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int boundaryEndIdx = TA_MIDPRICE_Lookback(14);
        if (boundaryEndIdx < 0) boundaryEndIdx = 0;
        if (boundaryEndIdx > 255) boundaryEndIdx = 255;
        double outReal[256] = {0.0};
        code = TA_MIDPRICE(0, boundaryEndIdx, High, Low, 14, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_seeded_mid_price") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        double outReal[256] = {0.0};
        code = TA_MIDPRICE(0, 127, High, Low, 14, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_variant_mid_price") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        double outReal[256] = {0.0};
        code = TA_MIDPRICE(0, 127, High, Low, 5, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_default_min") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        double outReal[256] = {0.0};
        code = TA_MIN(0, 15, inReal, 30, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_boundary_min") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int boundaryEndIdx = TA_MIN_Lookback(30);
        if (boundaryEndIdx < 0) boundaryEndIdx = 0;
        if (boundaryEndIdx > 255) boundaryEndIdx = 255;
        double outReal[256] = {0.0};
        code = TA_MIN(0, boundaryEndIdx, inReal, 30, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_seeded_min") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        double outReal[256] = {0.0};
        code = TA_MIN(0, 127, inReal, 30, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_variant_min") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        double outReal[256] = {0.0};
        code = TA_MIN(0, 127, inReal, 5, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_default_min_index") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int outInteger[256] = {0};
        code = TA_MININDEX(0, 15, inReal, 30, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_boundary_min_index") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int boundaryEndIdx = TA_MININDEX_Lookback(30);
        if (boundaryEndIdx < 0) boundaryEndIdx = 0;
        if (boundaryEndIdx > 255) boundaryEndIdx = 255;
        int outInteger[256] = {0};
        code = TA_MININDEX(0, boundaryEndIdx, inReal, 30, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_seeded_min_index") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        int outInteger[256] = {0};
        code = TA_MININDEX(0, 127, inReal, 30, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_variant_min_index") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        int outInteger[256] = {0};
        code = TA_MININDEX(0, 127, inReal, 5, &outBegIdx, &outNbElement, outInteger);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outInteger[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_default_min_max") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        double outMin[256] = {0.0};
        double outMax[256] = {0.0};
        code = TA_MINMAX(0, 15, inReal, 30, &outBegIdx, &outNbElement, outMin, outMax);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %.17g", outMin[i]);
        }
        printf(" |");
        for (int i = 0; i < outNbElement; i++) {
            printf(" %.17g", outMax[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_boundary_min_max") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int boundaryEndIdx = TA_MINMAX_Lookback(30);
        if (boundaryEndIdx < 0) boundaryEndIdx = 0;
        if (boundaryEndIdx > 255) boundaryEndIdx = 255;
        double outMin[256] = {0.0};
        double outMax[256] = {0.0};
        code = TA_MINMAX(0, boundaryEndIdx, inReal, 30, &outBegIdx, &outNbElement, outMin, outMax);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %.17g", outMin[i]);
        }
        printf(" |");
        for (int i = 0; i < outNbElement; i++) {
            printf(" %.17g", outMax[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_seeded_min_max") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        double outMin[256] = {0.0};
        double outMax[256] = {0.0};
        code = TA_MINMAX(0, 127, inReal, 30, &outBegIdx, &outNbElement, outMin, outMax);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %.17g", outMin[i]);
        }
        printf(" |");
        for (int i = 0; i < outNbElement; i++) {
            printf(" %.17g", outMax[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_variant_min_max") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        double outMin[256] = {0.0};
        double outMax[256] = {0.0};
        code = TA_MINMAX(0, 127, inReal, 5, &outBegIdx, &outNbElement, outMin, outMax);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %.17g", outMin[i]);
        }
        printf(" |");
        for (int i = 0; i < outNbElement; i++) {
            printf(" %.17g", outMax[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_default_min_max_index") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int outMinIdx[256] = {0};
        int outMaxIdx[256] = {0};
        code = TA_MINMAXINDEX(0, 15, inReal, 30, &outBegIdx, &outNbElement, outMinIdx, outMaxIdx);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outMinIdx[i]);
        }
        printf(" |");
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outMaxIdx[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_boundary_min_max_index") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int boundaryEndIdx = TA_MINMAXINDEX_Lookback(30);
        if (boundaryEndIdx < 0) boundaryEndIdx = 0;
        if (boundaryEndIdx > 255) boundaryEndIdx = 255;
        int outMinIdx[256] = {0};
        int outMaxIdx[256] = {0};
        code = TA_MINMAXINDEX(0, boundaryEndIdx, inReal, 30, &outBegIdx, &outNbElement, outMinIdx, outMaxIdx);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outMinIdx[i]);
        }
        printf(" |");
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outMaxIdx[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_seeded_min_max_index") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        int outMinIdx[256] = {0};
        int outMaxIdx[256] = {0};
        code = TA_MINMAXINDEX(0, 127, inReal, 30, &outBegIdx, &outNbElement, outMinIdx, outMaxIdx);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outMinIdx[i]);
        }
        printf(" |");
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outMaxIdx[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_variant_min_max_index") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        int outMinIdx[256] = {0};
        int outMaxIdx[256] = {0};
        code = TA_MINMAXINDEX(0, 127, inReal, 5, &outBegIdx, &outNbElement, outMinIdx, outMaxIdx);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outMinIdx[i]);
        }
        printf(" |");
        for (int i = 0; i < outNbElement; i++) {
            printf(" %d", outMaxIdx[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_default_minus_di") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        double outReal[256] = {0.0};
        code = TA_MINUS_DI(0, 15, High, Low, Close, 14, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_boundary_minus_di") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int boundaryEndIdx = TA_MINUS_DI_Lookback(14);
        if (boundaryEndIdx < 0) boundaryEndIdx = 0;
        if (boundaryEndIdx > 255) boundaryEndIdx = 255;
        double outReal[256] = {0.0};
        code = TA_MINUS_DI(0, boundaryEndIdx, High, Low, Close, 14, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_seeded_minus_di") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        double outReal[256] = {0.0};
        code = TA_MINUS_DI(0, 127, High, Low, Close, 14, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_variant_minus_di") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        double outReal[256] = {0.0};
        code = TA_MINUS_DI(0, 127, High, Low, Close, 5, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_default_minus_dm") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        double outReal[256] = {0.0};
        code = TA_MINUS_DM(0, 15, High, Low, 14, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_boundary_minus_dm") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int boundaryEndIdx = TA_MINUS_DM_Lookback(14);
        if (boundaryEndIdx < 0) boundaryEndIdx = 0;
        if (boundaryEndIdx > 255) boundaryEndIdx = 255;
        double outReal[256] = {0.0};
        code = TA_MINUS_DM(0, boundaryEndIdx, High, Low, 14, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_seeded_minus_dm") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        double outReal[256] = {0.0};
        code = TA_MINUS_DM(0, 127, High, Low, 14, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_variant_minus_dm") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        double outReal[256] = {0.0};
        code = TA_MINUS_DM(0, 127, High, Low, 5, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_default_mom") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        double outReal[256] = {0.0};
        code = TA_MOM(0, 15, inReal, 10, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_boundary_mom") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int boundaryEndIdx = TA_MOM_Lookback(10);
        if (boundaryEndIdx < 0) boundaryEndIdx = 0;
        if (boundaryEndIdx > 255) boundaryEndIdx = 255;
        double outReal[256] = {0.0};
        code = TA_MOM(0, boundaryEndIdx, inReal, 10, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_seeded_mom") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        double outReal[256] = {0.0};
        code = TA_MOM(0, 127, inReal, 10, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_variant_mom") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        double outReal[256] = {0.0};
        code = TA_MOM(0, 127, inReal, 5, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_default_mult") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        double outReal[256] = {0.0};
        code = TA_MULT(0, 15, inReal0, inReal1, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_boundary_mult") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int boundaryEndIdx = TA_MULT_Lookback();
        if (boundaryEndIdx < 0) boundaryEndIdx = 0;
        if (boundaryEndIdx > 255) boundaryEndIdx = 255;
        double outReal[256] = {0.0};
        code = TA_MULT(0, boundaryEndIdx, inReal0, inReal1, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_seeded_mult") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        double outReal[256] = {0.0};
        code = TA_MULT(0, 127, inReal0, inReal1, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_variant_mult") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        double outReal[256] = {0.0};
        code = TA_MULT(0, 127, inReal0, inReal1, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_default_natr") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        double outReal[256] = {0.0};
        code = TA_NATR(0, 15, High, Low, Close, 14, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_boundary_natr") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int boundaryEndIdx = TA_NATR_Lookback(14);
        if (boundaryEndIdx < 0) boundaryEndIdx = 0;
        if (boundaryEndIdx > 255) boundaryEndIdx = 255;
        double outReal[256] = {0.0};
        code = TA_NATR(0, boundaryEndIdx, High, Low, Close, 14, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_seeded_natr") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        double outReal[256] = {0.0};
        code = TA_NATR(0, 127, High, Low, Close, 14, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_variant_natr") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        double outReal[256] = {0.0};
        code = TA_NATR(0, 127, High, Low, Close, 5, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_default_obv") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        double outReal[256] = {0.0};
        code = TA_OBV(0, 15, inReal, Volume, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_boundary_obv") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int boundaryEndIdx = TA_OBV_Lookback();
        if (boundaryEndIdx < 0) boundaryEndIdx = 0;
        if (boundaryEndIdx > 255) boundaryEndIdx = 255;
        double outReal[256] = {0.0};
        code = TA_OBV(0, boundaryEndIdx, inReal, Volume, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_seeded_obv") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        double outReal[256] = {0.0};
        code = TA_OBV(0, 127, inReal, Volume, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_variant_obv") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        double outReal[256] = {0.0};
        code = TA_OBV(0, 127, inReal, Volume, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_default_plus_di") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        double outReal[256] = {0.0};
        code = TA_PLUS_DI(0, 15, High, Low, Close, 14, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_boundary_plus_di") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int boundaryEndIdx = TA_PLUS_DI_Lookback(14);
        if (boundaryEndIdx < 0) boundaryEndIdx = 0;
        if (boundaryEndIdx > 255) boundaryEndIdx = 255;
        double outReal[256] = {0.0};
        code = TA_PLUS_DI(0, boundaryEndIdx, High, Low, Close, 14, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_seeded_plus_di") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        double outReal[256] = {0.0};
        code = TA_PLUS_DI(0, 127, High, Low, Close, 14, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_variant_plus_di") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        double outReal[256] = {0.0};
        code = TA_PLUS_DI(0, 127, High, Low, Close, 5, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_default_plus_dm") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        double outReal[256] = {0.0};
        code = TA_PLUS_DM(0, 15, High, Low, 14, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_boundary_plus_dm") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int boundaryEndIdx = TA_PLUS_DM_Lookback(14);
        if (boundaryEndIdx < 0) boundaryEndIdx = 0;
        if (boundaryEndIdx > 255) boundaryEndIdx = 255;
        double outReal[256] = {0.0};
        code = TA_PLUS_DM(0, boundaryEndIdx, High, Low, 14, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_seeded_plus_dm") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        double outReal[256] = {0.0};
        code = TA_PLUS_DM(0, 127, High, Low, 14, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_variant_plus_dm") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        double outReal[256] = {0.0};
        code = TA_PLUS_DM(0, 127, High, Low, 5, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_default_ppo") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        double outReal[256] = {0.0};
        code = TA_PPO(0, 15, inReal, 12, 26, 0, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_boundary_ppo") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int boundaryEndIdx = TA_PPO_Lookback(12, 26, 0);
        if (boundaryEndIdx < 0) boundaryEndIdx = 0;
        if (boundaryEndIdx > 255) boundaryEndIdx = 255;
        double outReal[256] = {0.0};
        code = TA_PPO(0, boundaryEndIdx, inReal, 12, 26, 0, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_seeded_ppo") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        double outReal[256] = {0.0};
        code = TA_PPO(0, 127, inReal, 12, 26, 0, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_variant_ppo") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        double outReal[256] = {0.0};
        code = TA_PPO(0, 127, inReal, 8, 17, 1, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_default_roc") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        double outReal[256] = {0.0};
        code = TA_ROC(0, 15, inReal, 10, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_boundary_roc") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int boundaryEndIdx = TA_ROC_Lookback(10);
        if (boundaryEndIdx < 0) boundaryEndIdx = 0;
        if (boundaryEndIdx > 255) boundaryEndIdx = 255;
        double outReal[256] = {0.0};
        code = TA_ROC(0, boundaryEndIdx, inReal, 10, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_seeded_roc") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        double outReal[256] = {0.0};
        code = TA_ROC(0, 127, inReal, 10, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_variant_roc") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        double outReal[256] = {0.0};
        code = TA_ROC(0, 127, inReal, 5, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_default_roc_p") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        double outReal[256] = {0.0};
        code = TA_ROCP(0, 15, inReal, 10, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_boundary_roc_p") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int boundaryEndIdx = TA_ROCP_Lookback(10);
        if (boundaryEndIdx < 0) boundaryEndIdx = 0;
        if (boundaryEndIdx > 255) boundaryEndIdx = 255;
        double outReal[256] = {0.0};
        code = TA_ROCP(0, boundaryEndIdx, inReal, 10, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_seeded_roc_p") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        double outReal[256] = {0.0};
        code = TA_ROCP(0, 127, inReal, 10, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_variant_roc_p") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        double outReal[256] = {0.0};
        code = TA_ROCP(0, 127, inReal, 5, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_default_roc_r") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        double outReal[256] = {0.0};
        code = TA_ROCR(0, 15, inReal, 10, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_boundary_roc_r") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int boundaryEndIdx = TA_ROCR_Lookback(10);
        if (boundaryEndIdx < 0) boundaryEndIdx = 0;
        if (boundaryEndIdx > 255) boundaryEndIdx = 255;
        double outReal[256] = {0.0};
        code = TA_ROCR(0, boundaryEndIdx, inReal, 10, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_seeded_roc_r") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        double outReal[256] = {0.0};
        code = TA_ROCR(0, 127, inReal, 10, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_variant_roc_r") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        double outReal[256] = {0.0};
        code = TA_ROCR(0, 127, inReal, 5, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_default_roc_r100") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        double outReal[256] = {0.0};
        code = TA_ROCR100(0, 15, inReal, 10, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_boundary_roc_r100") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int boundaryEndIdx = TA_ROCR100_Lookback(10);
        if (boundaryEndIdx < 0) boundaryEndIdx = 0;
        if (boundaryEndIdx > 255) boundaryEndIdx = 255;
        double outReal[256] = {0.0};
        code = TA_ROCR100(0, boundaryEndIdx, inReal, 10, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_seeded_roc_r100") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        double outReal[256] = {0.0};
        code = TA_ROCR100(0, 127, inReal, 10, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_variant_roc_r100") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        double outReal[256] = {0.0};
        code = TA_ROCR100(0, 127, inReal, 5, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_default_rsi") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        double outReal[256] = {0.0};
        code = TA_RSI(0, 15, inReal, 14, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_boundary_rsi") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int boundaryEndIdx = TA_RSI_Lookback(14);
        if (boundaryEndIdx < 0) boundaryEndIdx = 0;
        if (boundaryEndIdx > 255) boundaryEndIdx = 255;
        double outReal[256] = {0.0};
        code = TA_RSI(0, boundaryEndIdx, inReal, 14, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_seeded_rsi") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        double outReal[256] = {0.0};
        code = TA_RSI(0, 127, inReal, 14, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_variant_rsi") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        double outReal[256] = {0.0};
        code = TA_RSI(0, 127, inReal, 5, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_default_sar") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        double outReal[256] = {0.0};
        code = TA_SAR(0, 15, High, Low, 2.000000e-2, 2.000000e-1, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_boundary_sar") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int boundaryEndIdx = TA_SAR_Lookback(2.000000e-2, 2.000000e-1);
        if (boundaryEndIdx < 0) boundaryEndIdx = 0;
        if (boundaryEndIdx > 255) boundaryEndIdx = 255;
        double outReal[256] = {0.0};
        code = TA_SAR(0, boundaryEndIdx, High, Low, 2.000000e-2, 2.000000e-1, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_seeded_sar") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        double outReal[256] = {0.0};
        code = TA_SAR(0, 127, High, Low, 2.000000e-2, 2.000000e-1, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_variant_sar") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        double outReal[256] = {0.0};
        code = TA_SAR(0, 127, High, Low, 2.000000e-2, 2.000000e-1, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_default_sarext") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        double outReal[256] = {0.0};
        code = TA_SAREXT(0, 15, High, Low, 0.000000e+0, 0.000000e+0, 2.000000e-2, 2.000000e-2, 2.000000e-1, 2.000000e-2, 2.000000e-2, 2.000000e-1, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_boundary_sarext") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int boundaryEndIdx = TA_SAREXT_Lookback(0.000000e+0, 0.000000e+0, 2.000000e-2, 2.000000e-2, 2.000000e-1, 2.000000e-2, 2.000000e-2, 2.000000e-1);
        if (boundaryEndIdx < 0) boundaryEndIdx = 0;
        if (boundaryEndIdx > 255) boundaryEndIdx = 255;
        double outReal[256] = {0.0};
        code = TA_SAREXT(0, boundaryEndIdx, High, Low, 0.000000e+0, 0.000000e+0, 2.000000e-2, 2.000000e-2, 2.000000e-1, 2.000000e-2, 2.000000e-2, 2.000000e-1, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_seeded_sarext") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        double outReal[256] = {0.0};
        code = TA_SAREXT(0, 127, High, Low, 0.000000e+0, 0.000000e+0, 2.000000e-2, 2.000000e-2, 2.000000e-1, 2.000000e-2, 2.000000e-2, 2.000000e-1, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_variant_sarext") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        double outReal[256] = {0.0};
        code = TA_SAREXT(0, 127, High, Low, 0.000000e+0, 0.000000e+0, 2.000000e-2, 2.000000e-2, 2.000000e-1, 2.000000e-2, 2.000000e-2, 2.000000e-1, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_default_sin") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        double outReal[256] = {0.0};
        code = TA_SIN(0, 15, inReal, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_boundary_sin") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int boundaryEndIdx = TA_SIN_Lookback();
        if (boundaryEndIdx < 0) boundaryEndIdx = 0;
        if (boundaryEndIdx > 255) boundaryEndIdx = 255;
        double outReal[256] = {0.0};
        code = TA_SIN(0, boundaryEndIdx, inReal, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_seeded_sin") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        double outReal[256] = {0.0};
        code = TA_SIN(0, 127, inReal, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_variant_sin") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        double outReal[256] = {0.0};
        code = TA_SIN(0, 127, inReal, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_default_sinh") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        double outReal[256] = {0.0};
        code = TA_SINH(0, 15, inReal, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_boundary_sinh") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int boundaryEndIdx = TA_SINH_Lookback();
        if (boundaryEndIdx < 0) boundaryEndIdx = 0;
        if (boundaryEndIdx > 255) boundaryEndIdx = 255;
        double outReal[256] = {0.0};
        code = TA_SINH(0, boundaryEndIdx, inReal, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_seeded_sinh") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        double outReal[256] = {0.0};
        code = TA_SINH(0, 127, inReal, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_variant_sinh") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        double outReal[256] = {0.0};
        code = TA_SINH(0, 127, inReal, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_default_sma") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        double outReal[256] = {0.0};
        code = TA_SMA(0, 15, inReal, 30, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_boundary_sma") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int boundaryEndIdx = TA_SMA_Lookback(30);
        if (boundaryEndIdx < 0) boundaryEndIdx = 0;
        if (boundaryEndIdx > 255) boundaryEndIdx = 255;
        double outReal[256] = {0.0};
        code = TA_SMA(0, boundaryEndIdx, inReal, 30, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_seeded_sma") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        double outReal[256] = {0.0};
        code = TA_SMA(0, 127, inReal, 30, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_variant_sma") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        double outReal[256] = {0.0};
        code = TA_SMA(0, 127, inReal, 5, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_default_sqrt") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        double outReal[256] = {0.0};
        code = TA_SQRT(0, 15, inReal, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_boundary_sqrt") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int boundaryEndIdx = TA_SQRT_Lookback();
        if (boundaryEndIdx < 0) boundaryEndIdx = 0;
        if (boundaryEndIdx > 255) boundaryEndIdx = 255;
        double outReal[256] = {0.0};
        code = TA_SQRT(0, boundaryEndIdx, inReal, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_seeded_sqrt") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        double outReal[256] = {0.0};
        code = TA_SQRT(0, 127, inReal, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_variant_sqrt") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        double outReal[256] = {0.0};
        code = TA_SQRT(0, 127, inReal, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_default_std_dev") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        double outReal[256] = {0.0};
        code = TA_STDDEV(0, 15, inReal, 5, 1.000000e+0, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_boundary_std_dev") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int boundaryEndIdx = TA_STDDEV_Lookback(5, 1.000000e+0);
        if (boundaryEndIdx < 0) boundaryEndIdx = 0;
        if (boundaryEndIdx > 255) boundaryEndIdx = 255;
        double outReal[256] = {0.0};
        code = TA_STDDEV(0, boundaryEndIdx, inReal, 5, 1.000000e+0, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_seeded_std_dev") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        double outReal[256] = {0.0};
        code = TA_STDDEV(0, 127, inReal, 5, 1.000000e+0, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_variant_std_dev") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        double outReal[256] = {0.0};
        code = TA_STDDEV(0, 127, inReal, 5, 1.000000e+0, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_default_stoch") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        double outSlowK[256] = {0.0};
        double outSlowD[256] = {0.0};
        code = TA_STOCH(0, 15, High, Low, Close, 5, 3, 0, 3, 0, &outBegIdx, &outNbElement, outSlowK, outSlowD);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %.17g", outSlowK[i]);
        }
        printf(" |");
        for (int i = 0; i < outNbElement; i++) {
            printf(" %.17g", outSlowD[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_boundary_stoch") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int boundaryEndIdx = TA_STOCH_Lookback(5, 3, 0, 3, 0);
        if (boundaryEndIdx < 0) boundaryEndIdx = 0;
        if (boundaryEndIdx > 255) boundaryEndIdx = 255;
        double outSlowK[256] = {0.0};
        double outSlowD[256] = {0.0};
        code = TA_STOCH(0, boundaryEndIdx, High, Low, Close, 5, 3, 0, 3, 0, &outBegIdx, &outNbElement, outSlowK, outSlowD);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %.17g", outSlowK[i]);
        }
        printf(" |");
        for (int i = 0; i < outNbElement; i++) {
            printf(" %.17g", outSlowD[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_seeded_stoch") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        double outSlowK[256] = {0.0};
        double outSlowD[256] = {0.0};
        code = TA_STOCH(0, 127, High, Low, Close, 5, 3, 0, 3, 0, &outBegIdx, &outNbElement, outSlowK, outSlowD);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %.17g", outSlowK[i]);
        }
        printf(" |");
        for (int i = 0; i < outNbElement; i++) {
            printf(" %.17g", outSlowD[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_variant_stoch") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        double outSlowK[256] = {0.0};
        double outSlowD[256] = {0.0};
        code = TA_STOCH(0, 127, High, Low, Close, 5, 4, 0, 3, 0, &outBegIdx, &outNbElement, outSlowK, outSlowD);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %.17g", outSlowK[i]);
        }
        printf(" |");
        for (int i = 0; i < outNbElement; i++) {
            printf(" %.17g", outSlowD[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_default_stochf") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        double outFastK[256] = {0.0};
        double outFastD[256] = {0.0};
        code = TA_STOCHF(0, 15, High, Low, Close, 5, 3, 0, &outBegIdx, &outNbElement, outFastK, outFastD);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %.17g", outFastK[i]);
        }
        printf(" |");
        for (int i = 0; i < outNbElement; i++) {
            printf(" %.17g", outFastD[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_boundary_stochf") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int boundaryEndIdx = TA_STOCHF_Lookback(5, 3, 0);
        if (boundaryEndIdx < 0) boundaryEndIdx = 0;
        if (boundaryEndIdx > 255) boundaryEndIdx = 255;
        double outFastK[256] = {0.0};
        double outFastD[256] = {0.0};
        code = TA_STOCHF(0, boundaryEndIdx, High, Low, Close, 5, 3, 0, &outBegIdx, &outNbElement, outFastK, outFastD);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %.17g", outFastK[i]);
        }
        printf(" |");
        for (int i = 0; i < outNbElement; i++) {
            printf(" %.17g", outFastD[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_seeded_stochf") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        double outFastK[256] = {0.0};
        double outFastD[256] = {0.0};
        code = TA_STOCHF(0, 127, High, Low, Close, 5, 3, 0, &outBegIdx, &outNbElement, outFastK, outFastD);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %.17g", outFastK[i]);
        }
        printf(" |");
        for (int i = 0; i < outNbElement; i++) {
            printf(" %.17g", outFastD[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_variant_stochf") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        double outFastK[256] = {0.0};
        double outFastD[256] = {0.0};
        code = TA_STOCHF(0, 127, High, Low, Close, 5, 4, 0, &outBegIdx, &outNbElement, outFastK, outFastD);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %.17g", outFastK[i]);
        }
        printf(" |");
        for (int i = 0; i < outNbElement; i++) {
            printf(" %.17g", outFastD[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_default_stochrsi") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        double outFastK[256] = {0.0};
        double outFastD[256] = {0.0};
        code = TA_STOCHRSI(0, 15, inReal, 14, 5, 3, 0, &outBegIdx, &outNbElement, outFastK, outFastD);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %.17g", outFastK[i]);
        }
        printf(" |");
        for (int i = 0; i < outNbElement; i++) {
            printf(" %.17g", outFastD[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_boundary_stochrsi") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int boundaryEndIdx = TA_STOCHRSI_Lookback(14, 5, 3, 0);
        if (boundaryEndIdx < 0) boundaryEndIdx = 0;
        if (boundaryEndIdx > 255) boundaryEndIdx = 255;
        double outFastK[256] = {0.0};
        double outFastD[256] = {0.0};
        code = TA_STOCHRSI(0, boundaryEndIdx, inReal, 14, 5, 3, 0, &outBegIdx, &outNbElement, outFastK, outFastD);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %.17g", outFastK[i]);
        }
        printf(" |");
        for (int i = 0; i < outNbElement; i++) {
            printf(" %.17g", outFastD[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_seeded_stochrsi") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        double outFastK[256] = {0.0};
        double outFastD[256] = {0.0};
        code = TA_STOCHRSI(0, 127, inReal, 14, 5, 3, 0, &outBegIdx, &outNbElement, outFastK, outFastD);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %.17g", outFastK[i]);
        }
        printf(" |");
        for (int i = 0; i < outNbElement; i++) {
            printf(" %.17g", outFastD[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_variant_stochrsi") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        double outFastK[256] = {0.0};
        double outFastD[256] = {0.0};
        code = TA_STOCHRSI(0, 127, inReal, 5, 5, 4, 0, &outBegIdx, &outNbElement, outFastK, outFastD);
        printf("%d %d %d", (int)code, outBegIdx, outNbElement);
        for (int i = 0; i < outNbElement; i++) {
            printf(" %.17g", outFastK[i]);
        }
        printf(" |");
        for (int i = 0; i < outNbElement; i++) {
            printf(" %.17g", outFastD[i]);
        }
        printf("\n");
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_default_sub") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        double outReal[256] = {0.0};
        code = TA_SUB(0, 15, inReal0, inReal1, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_boundary_sub") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int boundaryEndIdx = TA_SUB_Lookback();
        if (boundaryEndIdx < 0) boundaryEndIdx = 0;
        if (boundaryEndIdx > 255) boundaryEndIdx = 255;
        double outReal[256] = {0.0};
        code = TA_SUB(0, boundaryEndIdx, inReal0, inReal1, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_seeded_sub") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        double outReal[256] = {0.0};
        code = TA_SUB(0, 127, inReal0, inReal1, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_variant_sub") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        double outReal[256] = {0.0};
        code = TA_SUB(0, 127, inReal0, inReal1, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_default_sum") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        double outReal[256] = {0.0};
        code = TA_SUM(0, 15, inReal, 30, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_boundary_sum") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int boundaryEndIdx = TA_SUM_Lookback(30);
        if (boundaryEndIdx < 0) boundaryEndIdx = 0;
        if (boundaryEndIdx > 255) boundaryEndIdx = 255;
        double outReal[256] = {0.0};
        code = TA_SUM(0, boundaryEndIdx, inReal, 30, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_seeded_sum") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        double outReal[256] = {0.0};
        code = TA_SUM(0, 127, inReal, 30, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_variant_sum") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        double outReal[256] = {0.0};
        code = TA_SUM(0, 127, inReal, 5, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_default_t3") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        double outReal[256] = {0.0};
        code = TA_T3(0, 15, inReal, 5, 7.000000e-1, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_boundary_t3") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int boundaryEndIdx = TA_T3_Lookback(5, 7.000000e-1);
        if (boundaryEndIdx < 0) boundaryEndIdx = 0;
        if (boundaryEndIdx > 255) boundaryEndIdx = 255;
        double outReal[256] = {0.0};
        code = TA_T3(0, boundaryEndIdx, inReal, 5, 7.000000e-1, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_seeded_t3") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        double outReal[256] = {0.0};
        code = TA_T3(0, 127, inReal, 5, 7.000000e-1, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_variant_t3") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        double outReal[256] = {0.0};
        code = TA_T3(0, 127, inReal, 5, 7.000000e-1, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_default_tan") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        double outReal[256] = {0.0};
        code = TA_TAN(0, 15, inReal, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_boundary_tan") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int boundaryEndIdx = TA_TAN_Lookback();
        if (boundaryEndIdx < 0) boundaryEndIdx = 0;
        if (boundaryEndIdx > 255) boundaryEndIdx = 255;
        double outReal[256] = {0.0};
        code = TA_TAN(0, boundaryEndIdx, inReal, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_seeded_tan") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        double outReal[256] = {0.0};
        code = TA_TAN(0, 127, inReal, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_variant_tan") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        double outReal[256] = {0.0};
        code = TA_TAN(0, 127, inReal, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_default_tanh") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        double outReal[256] = {0.0};
        code = TA_TANH(0, 15, inReal, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_boundary_tanh") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int boundaryEndIdx = TA_TANH_Lookback();
        if (boundaryEndIdx < 0) boundaryEndIdx = 0;
        if (boundaryEndIdx > 255) boundaryEndIdx = 255;
        double outReal[256] = {0.0};
        code = TA_TANH(0, boundaryEndIdx, inReal, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_seeded_tanh") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        double outReal[256] = {0.0};
        code = TA_TANH(0, 127, inReal, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_variant_tanh") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        double outReal[256] = {0.0};
        code = TA_TANH(0, 127, inReal, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_default_tema") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        double outReal[256] = {0.0};
        code = TA_TEMA(0, 15, inReal, 30, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_boundary_tema") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int boundaryEndIdx = TA_TEMA_Lookback(30);
        if (boundaryEndIdx < 0) boundaryEndIdx = 0;
        if (boundaryEndIdx > 255) boundaryEndIdx = 255;
        double outReal[256] = {0.0};
        code = TA_TEMA(0, boundaryEndIdx, inReal, 30, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_seeded_tema") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        double outReal[256] = {0.0};
        code = TA_TEMA(0, 127, inReal, 30, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_variant_tema") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        double outReal[256] = {0.0};
        code = TA_TEMA(0, 127, inReal, 5, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_default_true_range") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        double outReal[256] = {0.0};
        code = TA_TRANGE(0, 15, High, Low, Close, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_boundary_true_range") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int boundaryEndIdx = TA_TRANGE_Lookback();
        if (boundaryEndIdx < 0) boundaryEndIdx = 0;
        if (boundaryEndIdx > 255) boundaryEndIdx = 255;
        double outReal[256] = {0.0};
        code = TA_TRANGE(0, boundaryEndIdx, High, Low, Close, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_seeded_true_range") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        double outReal[256] = {0.0};
        code = TA_TRANGE(0, 127, High, Low, Close, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_variant_true_range") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        double outReal[256] = {0.0};
        code = TA_TRANGE(0, 127, High, Low, Close, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_default_trima") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        double outReal[256] = {0.0};
        code = TA_TRIMA(0, 15, inReal, 30, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_boundary_trima") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int boundaryEndIdx = TA_TRIMA_Lookback(30);
        if (boundaryEndIdx < 0) boundaryEndIdx = 0;
        if (boundaryEndIdx > 255) boundaryEndIdx = 255;
        double outReal[256] = {0.0};
        code = TA_TRIMA(0, boundaryEndIdx, inReal, 30, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_seeded_trima") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        double outReal[256] = {0.0};
        code = TA_TRIMA(0, 127, inReal, 30, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_variant_trima") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        double outReal[256] = {0.0};
        code = TA_TRIMA(0, 127, inReal, 5, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_default_trix") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        double outReal[256] = {0.0};
        code = TA_TRIX(0, 15, inReal, 30, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_boundary_trix") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int boundaryEndIdx = TA_TRIX_Lookback(30);
        if (boundaryEndIdx < 0) boundaryEndIdx = 0;
        if (boundaryEndIdx > 255) boundaryEndIdx = 255;
        double outReal[256] = {0.0};
        code = TA_TRIX(0, boundaryEndIdx, inReal, 30, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_seeded_trix") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        double outReal[256] = {0.0};
        code = TA_TRIX(0, 127, inReal, 30, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_variant_trix") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        double outReal[256] = {0.0};
        code = TA_TRIX(0, 127, inReal, 5, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_default_tsf") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        double outReal[256] = {0.0};
        code = TA_TSF(0, 15, inReal, 14, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_boundary_tsf") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int boundaryEndIdx = TA_TSF_Lookback(14);
        if (boundaryEndIdx < 0) boundaryEndIdx = 0;
        if (boundaryEndIdx > 255) boundaryEndIdx = 255;
        double outReal[256] = {0.0};
        code = TA_TSF(0, boundaryEndIdx, inReal, 14, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_seeded_tsf") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        double outReal[256] = {0.0};
        code = TA_TSF(0, 127, inReal, 14, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_variant_tsf") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        double outReal[256] = {0.0};
        code = TA_TSF(0, 127, inReal, 5, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_default_typ_price") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        double outReal[256] = {0.0};
        code = TA_TYPPRICE(0, 15, High, Low, Close, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_boundary_typ_price") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int boundaryEndIdx = TA_TYPPRICE_Lookback();
        if (boundaryEndIdx < 0) boundaryEndIdx = 0;
        if (boundaryEndIdx > 255) boundaryEndIdx = 255;
        double outReal[256] = {0.0};
        code = TA_TYPPRICE(0, boundaryEndIdx, High, Low, Close, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_seeded_typ_price") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        double outReal[256] = {0.0};
        code = TA_TYPPRICE(0, 127, High, Low, Close, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_variant_typ_price") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        double outReal[256] = {0.0};
        code = TA_TYPPRICE(0, 127, High, Low, Close, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_default_ultosc") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        double outReal[256] = {0.0};
        code = TA_ULTOSC(0, 15, High, Low, Close, 7, 14, 28, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_boundary_ultosc") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int boundaryEndIdx = TA_ULTOSC_Lookback(7, 14, 28);
        if (boundaryEndIdx < 0) boundaryEndIdx = 0;
        if (boundaryEndIdx > 255) boundaryEndIdx = 255;
        double outReal[256] = {0.0};
        code = TA_ULTOSC(0, boundaryEndIdx, High, Low, Close, 7, 14, 28, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_seeded_ultosc") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        double outReal[256] = {0.0};
        code = TA_ULTOSC(0, 127, High, Low, Close, 7, 14, 28, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_variant_ultosc") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        double outReal[256] = {0.0};
        code = TA_ULTOSC(0, 127, High, Low, Close, 7, 14, 28, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_default_variance") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        double outReal[256] = {0.0};
        code = TA_VAR(0, 15, inReal, 5, 1.000000e+0, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_boundary_variance") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int boundaryEndIdx = TA_VAR_Lookback(5, 1.000000e+0);
        if (boundaryEndIdx < 0) boundaryEndIdx = 0;
        if (boundaryEndIdx > 255) boundaryEndIdx = 255;
        double outReal[256] = {0.0};
        code = TA_VAR(0, boundaryEndIdx, inReal, 5, 1.000000e+0, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_seeded_variance") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        double outReal[256] = {0.0};
        code = TA_VAR(0, 127, inReal, 5, 1.000000e+0, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_variant_variance") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        double outReal[256] = {0.0};
        code = TA_VAR(0, 127, inReal, 5, 1.000000e+0, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_default_wcl_price") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        double outReal[256] = {0.0};
        code = TA_WCLPRICE(0, 15, High, Low, Close, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_boundary_wcl_price") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int boundaryEndIdx = TA_WCLPRICE_Lookback();
        if (boundaryEndIdx < 0) boundaryEndIdx = 0;
        if (boundaryEndIdx > 255) boundaryEndIdx = 255;
        double outReal[256] = {0.0};
        code = TA_WCLPRICE(0, boundaryEndIdx, High, Low, Close, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_seeded_wcl_price") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        double outReal[256] = {0.0};
        code = TA_WCLPRICE(0, 127, High, Low, Close, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_variant_wcl_price") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        double outReal[256] = {0.0};
        code = TA_WCLPRICE(0, 127, High, Low, Close, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_default_will_r") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        double outReal[256] = {0.0};
        code = TA_WILLR(0, 15, High, Low, Close, 14, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_boundary_will_r") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int boundaryEndIdx = TA_WILLR_Lookback(14);
        if (boundaryEndIdx < 0) boundaryEndIdx = 0;
        if (boundaryEndIdx > 255) boundaryEndIdx = 255;
        double outReal[256] = {0.0};
        code = TA_WILLR(0, boundaryEndIdx, High, Low, Close, 14, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_seeded_will_r") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        double outReal[256] = {0.0};
        code = TA_WILLR(0, 127, High, Low, Close, 14, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_variant_will_r") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        double outReal[256] = {0.0};
        code = TA_WILLR(0, 127, High, Low, Close, 5, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_default_wma") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        double outReal[256] = {0.0};
        code = TA_WMA(0, 15, inReal, 30, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_boundary_wma") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            inReal[i] = 1.0 + i * 0.5 + (i % 3) * 0.25;
            inReal0[i] = 2.0 + i * 0.5 + (i % 3) * 0.25;
            inReal1[i] = 1.0 + i * 0.25;
            Open[i] = 10.0 + i * 0.2;
            High[i] = 10.9 + i * 0.2 + (i % 4) * 0.1;
            Low[i] = 9.5 + i * 0.2;
            Close[i] = 10.6 + i * 0.2 + (i % 5) * 0.05;
            Volume[i] = 1000.0 + i * 25.0;
            inPeriods[i] = 2.0 + (i % 7);
        }
        int boundaryEndIdx = TA_WMA_Lookback(30);
        if (boundaryEndIdx < 0) boundaryEndIdx = 0;
        if (boundaryEndIdx > 255) boundaryEndIdx = 255;
        double outReal[256] = {0.0};
        code = TA_WMA(0, boundaryEndIdx, inReal, 30, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_seeded_wma") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        double outReal[256] = {0.0};
        code = TA_WMA(0, 127, inReal, 30, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
} else if (strcmp(argv[1], "matrix_variant_wma") == 0) {
        double inReal[256];
        double inReal0[256];
        double inReal1[256];
        double Open[256];
        double High[256];
        double Low[256];
        double Close[256];
        double Volume[256];
        double inPeriods[256];
        for (int i = 0; i < 256; i++) {
            double open = 50.0 + i * 0.1 + (((i * 17) % 11) - 5) * 0.07;
            double close = open + (((i * 19) % 9) - 4) * 0.05;
            inReal[i] = 20.0 + i * 0.13 + ((i * 17) % 11) * 0.17 - ((i * 7) % 5) * 0.11;
            inReal0[i] = 30.0 + i * 0.09 + ((i * 13) % 7) * 0.21 - ((i * 5) % 3) * 0.08;
            inReal1[i] = 5.0 + i * 0.04 + ((i * 11) % 9) * 0.06;
            Open[i] = open;
            Close[i] = close;
            High[i] = (open > close ? open : close) + 0.2 + (i % 5) * 0.03;
            Low[i] = (open < close ? open : close) - 0.2 - (i % 4) * 0.02;
            Volume[i] = 500.0 + ((i * 37) % 97) * 13.0 + i * 3.0;
            inPeriods[i] = 2.0 + ((i * 3) % 9);
        }
        double outReal[256] = {0.0};
        code = TA_WMA(0, 127, inReal, 5, &outBegIdx, &outNbElement, outReal);
        print_result(code, outBegIdx, outNbElement, outReal);
        TA_Shutdown();
        return 0;
    
